"""

chat.py

========

Production-grade RAG chat endpoint.



Retrieval pipeline (per query):

  1. Normalize query          – arabic_cleaner.clean_arabic_text()

  2. Hybrid search            – dense (ChromaDB) + sparse (BM25) + RRF fusion

  3. Rerank                   – cosine similarity (default) or LLM scoring

  4. Build context            – top-N chunks with source attribution

  5. Generate answer          – LLM with strict grounding prompt

  6. Log retrieval trace      – JSONL log for offline analysis



Extended API

------------

  POST /api/chat/

      Body: { question, top_k?, language_filter?, section_filter?,

              file_filter?, rerank_method? }



  GET  /api/chat/retrieval-stats

      Returns cache stats and BM25 index info.



  POST /api/chat/rebuild-index

      Force-rebuilds the BM25 index from ChromaDB.



  GET  /api/chat/retrieval-logs

      Returns the last N retrieval log entries.

"""



from __future__ import annotations



import logging

import os

import re

import time

from typing import List, Optional



import chromadb

from chromadb.config import Settings

from dotenv import load_dotenv

from fastapi import APIRouter, HTTPException, Query

from fastapi.responses import JSONResponse

from langchain_core.prompts import PromptTemplate

from langchain_openai import ChatOpenAI, OpenAIEmbeddings
from sentence_transformers import SentenceTransformer
from typing import List as _List

from pydantic import BaseModel, Field



from routes.retrieval.bm25_index import get_or_build_index, invalidate_index

from routes.retrieval.embedding_cache import get_default_cache

from routes.retrieval.evaluator import log_retrieval_result

from routes.retrieval.hybrid_search import hybrid_search, normalize_query

from routes.retrieval.query_understanding import understand_query

from routes.retrieval.reranker import rerank_results

from routes.course_catalog import get_catalog

from routes.arabic_cleaner import detect_language

load_dotenv()

logger = logging.getLogger(__name__)



# ---------------------------------------------------------------------------

# Router

# ---------------------------------------------------------------------------

router = APIRouter()



# ---------------------------------------------------------------------------

# ChromaDB

# ---------------------------------------------------------------------------

chroma_client = chromadb.PersistentClient(

    path="./chroma_db",

    settings=Settings(anonymized_telemetry=False),

)



COLLECTION_NAME = "university_regulations"



try:

    collection = chroma_client.get_collection(name=COLLECTION_NAME)

    logger.info(f"[chat] ChromaDB collection '{COLLECTION_NAME}' loaded "

                f"({collection.count()} documents)")

except Exception:

    collection = None

    logger.warning(f"[chat] Collection '{COLLECTION_NAME}' not found – "

                   "upload PDFs first.")



# ---------------------------------------------------------------------------

# Embeddings  – LOCAL model (must match upload.py to avoid dimension mismatch)
# ---------------------------------------------------------------------------
# IMPORTANT: upload.py indexes with all-MiniLM-L6-v2 (384 dim).
# Using any cloud model (ada-002 = 1536 dim) here would cause zero-relevance
# retrievals because ChromaDB rejects mismatched query vectors.
# Always keep this in sync with the model used in upload.py.

class _LocalHuggingFaceEmbeddings:
    """Local embedding wrapper – identical to the one in upload.py."""
    _EMBED_MODEL_NAME = "all-MiniLM-L6-v2"

    def __init__(self):
        logger.info(f"[chat] Loading local embedding model: {self._EMBED_MODEL_NAME}")
        try:
            self._model = SentenceTransformer(self._EMBED_MODEL_NAME, local_files_only=True)
        except Exception:
            logger.info("[chat] Model not in local cache – downloading from HF Hub")
            self._model = SentenceTransformer(self._EMBED_MODEL_NAME)
        logger.info("[chat] Local embedding model loaded (384 dim)")

    def embed_documents(self, texts: _List[str]) -> _List[_List[float]]:
        return self._model.encode(texts, show_progress_bar=False).tolist()

    def embed_query(self, text: str) -> _List[float]:
        return self._model.encode([text], show_progress_bar=False)[0].tolist()

try:
    embeddings = _LocalHuggingFaceEmbeddings()
except Exception as _emb_err:
    logger.error(f"[chat] FATAL: Could not load local embeddings: {_emb_err}")
    embeddings = None



# ---------------------------------------------------------------------------

# LLM

# ---------------------------------------------------------------------------

llm = ChatOpenAI(

    openai_api_key=os.getenv("OPENROUTER_API_KEY"),

    openai_api_base="https://openrouter.ai/api/v1",

    model_name=os.getenv("OPENROUTER_MODEL", "openai/gpt-oss-120b:free"),

    temperature=0.1,

    default_headers={

        "HTTP-Referer": os.getenv("OPENROUTER_REFERRER", "http://localhost:8000"),

        "X-Title": "Smart Campus RAG",

    },

)



# ---------------------------------------------------------------------------

# Embedding cache (singleton)

# ---------------------------------------------------------------------------

embedding_cache = get_default_cache()



# ---------------------------------------------------------------------------

# BM25 index (lazy-loaded on first request)

# ---------------------------------------------------------------------------

_bm25_index = None





def _get_bm25_index(force_rebuild: bool = False):

    """Lazy-load or rebuild the BM25 index."""

    global _bm25_index

    if collection is None:

        return None

    if force_rebuild or _bm25_index is None:

        _bm25_index = get_or_build_index(

            collection_name=COLLECTION_NAME,

            collection=collection,

            force_rebuild=force_rebuild,

        )

    return _bm25_index





# ---------------------------------------------------------------------------

# Prompt templates

# ---------------------------------------------------------------------------
# Prompt templates
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Main prompt – compact, few-shot, anti-hallucination
# ---------------------------------------------------------------------------
_PROMPT = PromptTemplate.from_template("""\
أنت مساعد أكاديمي دقيق جداً متخصص في لوائح جامعة المنوفية الأهلية.

معلومات عامة ثابتة (FAQ):
- رئيس جامعة المنوفية الأهلية (والمكلف بتسيير أعمالها) هو: أ.د/ أحمد القاصد. أجب بهذه المعلومة مباشرة إذا سُئلت عنه حتى لو لم تكن في السياق.
- قواعد التدريب العملي والميداني لكلية الحاسوب والذكاء الاصطناعي (أجب بها مباشرة إذا سُئلت عنها):
  • التدريب العملي والميداني إجباري للحصول على درجة البكالوريوس (متطلب تخرج).
  • مدته: 6 أسابيع (يعادل 4 ساعات تدريس فعلية) خلال الإجازة الصيفية.
  • شروط التسجيل: أن يكون الطالب قد اجتاز على الأقل 60% من الساعات المعتمدة اللازمة للتخرج (أي اجتياز 87 ساعة معتمدة)، وألا يكون مسجلاً بالفصل الدراسي الصيفي الذي يتم فيه أداء التدريب.
  • مكان التدريب: يجوز أداء التدريب داخل أو خارج الكلية (أو خارج الجمهورية بموافقة وترشيح مجلس إدارة البرنامج العلمي وموافقة مجلس الكلية والجامعة).
  • الإشراف والتقييم: يتم تحت إشراف عضو هيئة تدريس، ويلتزم الطالب بتقديم تقرير عن فترة تدريبه للمشرف.
  • الساعات والمجموع: التدريب مقرر بدون ساعات معتمدة (0 ساعة معتمدة) ولا يدخل في حساب المعدل التراكمي (CGPA).
- كليات جامعة المنوفية الأهلية (10 كليات)، أجب بها مباشرة إذا سُئلت عنها:
  1. كلية الطب والجراحة
  2. كلية طب الأسنان
  3. كلية العلاج الطبيعي
  4. كلية الصيدلة
  5. كلية الطب البيطري
  6. كلية تكنولوجيا العلوم الصحية
  7. كلية التمريض
  8. كلية الهندسة
  9. كلية الحاسوب والذكاء الاصطناعي
  10. كلية العلوم الإنسانية والاجتماعية
- طريقة حساب المعدل التراكمي (CGPA): إذا سُئلت عنها وكان السياق يحتوي على نص مشوه أو غير مقروء بسبب OCR، استخدم المعادلة التالية المستخرجة من اللوائح الرسمية:
  • نقاط المقرر = عدد الساعات المعتمدة × نقاط التقدير
  • GPA الفصلي = مجموع نقاط المقررات في الفصل ÷ مجموع الساعات المعتمدة في الفصل
  • CGPA التراكمي = مجموع نقاط كل المقررات التي اجتازها الطالب ÷ مجموع الساعات المسجلة
- قواعد تسجيل الساعات والمقررات بناءً على المعدل التراكمي (GPA) في كلية الحاسوب والذكاء الاصطناعي (أجب بها مباشرة إذا سُئلت عنها):
  • الطالب الحاصل على GPA أكبر من أو يساوي 2.0 (بما في ذلك معدل 2.5): الحد الأقصى لتسجيل الساعات هو **18 ساعة معتمدة** في الفصل الدراسي الأساسي (الخريف/الربيع)، والحد الأقصى في الفصل الصيفي هو **9 ساعات معتمدة** (أو **8 ساعات معتمدة** وفقاً للائحة الموحدة العامة).
  • الطالب الحاصل على GPA من 1.0 إلى أقل من 2.0: الحد الأقصى للتسجيل هو **15 ساعة معتمدة** في الفصل الأساسي.
  • الطالب الحاصل على GPA أقل من 1.0 (تحت الإنذار الأكاديمي): الحد الأقصى للتسجيل هو **12 ساعة معتمدة** في الفصل الأساسي.
  • الحد الأدنى للتسجيل في الفصل الأساسي: **9 ساعات معتمدة**.
  • لدواعي التخرج: يجوز لمجلس الكلية تجاوز الحد الأقصى للتسجيل للطالب الخريج على ألا يزيد عن **21 ساعة معتمدة** في الفصل الأساسي.
  • عدد المقررات (المواد): لحساب عدد المقررات التي يستطيع الطالب تسجيلها، قسّم الحد الأقصى للساعات المعتمدة على 3 (حيث أن متوسط المقرر الدراسي يعادل **3 ساعات معتمدة**). مثال: للطالب ذو GPA 2.5، يكون الحد الأقصى هو 18 ساعة معتمدة، وهو ما يعادل تسجيل **6 مواد** كحد أقصى.
  • الإنذار الأكاديمي: يوجه للطالب إنذار أكاديمي إذا انخفض معدله التراكمي (GPA) عن **2.0** في نهاية أي فصل دراسي رئيسي.
- متطلبات سابقة شاملة لكلية الحاسوب والذكاء الاصطناعي (أجب بها مباشرة إذا سُئلت):

  [برنامج إنترنت الأشياء وتحليل البيانات الضخمة - متطلبات الكلية الأساسية]
  • MBS107 رياضيات-2 → المتطلب: MBS105 رياضيات-1
  • MBS109 احصاء واحتماالت → المتطلب: MBS105 رياضيات-1
  • MBS210 طرق احصائية → المتطلب: MBS109 احصاء واحتماالت
  • BCS212 برمجة حاسبات-2 → المتطلب: BCS111 برمجة حاسبات-1
  • BCS113 تصميم منطقي → المتطلب: MBS108 مبادئ الإلكترونيات
  • BCS214 هياكل البيانات → المتطلب: BCS111 برمجة حاسبات-1
  • BCS417 معمارية وتنظيم الحاسبات → المتطلب: BCS113 تصميم منطقي
  • BCS219 تحليل وتصميم النظم → المتطلب: ICI115 مقدمة في علوم البيانات
  • BCS220 نظم تشغيل → المتطلب: BCS111 برمجة حاسبات-1
  • BCS221 هندسة البرمجيات → المتطلب: BCS111 برمجة حاسبات-1
  • BCS322 مقدمة في الذكاء الاصطناعي → المتطلب: BCS214 هياكل البيانات
  • BCS223 نظم قواعد البيانات → المتطلب: ICI115 مقدمة في علوم البيانات
  • BCS324 مستودعات وتنقيب عن البيانات → المتطلب: BCS223 نظم قواعد البيانات
  • BCS225 شبكات الحاسبات → المتطلب: MBS143 مقدمة في الحاسبات
  • BCS243 برمجة حاسبات-3 → المتطلب: BCS212 برمجة حاسبات-2
  • ICI115 مقدمة في علوم البيانات → المتطلب: MBS109 احصاء واحتماالت
  • ICI316 تصور البيانات → المتطلب: ICI115 مقدمة في علوم البيانات
  • ICI218 مقدمة الى البيانات الضخمة → المتطلب: ICI115 مقدمة في علوم البيانات
  • ICI226 مقدمة في انترنت الاشياء → المتطلب: BCS225 شبكات الحاسبات
  • ICI428 انترنت الاشياء في تكنولوجيا المحمول → المتطلب: ICI226 مقدمة في انترنت الاشياء
  • ICI334 برتوكوالت انترنت الاشياء → المتطلب: ICI226 مقدمة في انترنت الاشياء
  • ICI336 تحليل البيانات الضخمة → المتطلب: BCS223 نظم قواعد البيانات

  [برنامج إنترنت الأشياء - متطلبات التخصص الأساسية والاختيارية]
  • IOT327 الذكاء الاصطناعي المتقدم → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT429 الأمن السيبراني → المتطلب: BCS225 شبكات الحاسبات
  • IOT330 تحليل وتصميم الخوارزميات → المتطلب: BCS214 هياكل البيانات
  • IOT331 قواعد البيانات المتقدمة → المتطلب: BCS223 نظم قواعد البيانات
  • IOT432 تعلم الآلة Machine Learning → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT333 استرجاع المعلومات → المتطلب: BCS223 نظم قواعد البيانات
  • IOT435 التسويق الرقمي → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT437 الرؤيا بالحاسب Computer Vision → المتطلب: ICI316 تصور البيانات
  • IOT438 حسابات الإنترنت → المتطلب: BCS212 برمجة حاسبات-2
  • IOT439 اتصال الإنسان بالحاسب → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT240 برمجة المحمول → المتطلب: BCS212 برمجة حاسبات-2
  • IOT341 تحليل الاعمال → المتطلب: BCS219 تحليل وتصميم النظم
  • IOT442 مشروع التخرج → المتطلب: اجتياز 101 ساعة معتمدة على الأقل
  • IOT350 علوم البيانات المتقدمة (اختياري) → المتطلب: ICI115 مقدمة في علوم البيانات
  • IOT451 الشبكات العصبية والتعلم العميق (اختياري) → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT353 ادارة المعرفة (اختياري) → المتطلب: ICI115 مقدمة في علوم البيانات
  • IOT454 الحسابات عالية الاداء وحاسبات الكم (اختياري) → المتطلب: BCS417 معمارية وتنظيم الحاسبات
  • IOT455 النظم المدمجة (اختياري) → المتطلب: BCS417 معمارية وتنظيم الحاسبات
  • IOT356 موضوعات مختارة في أنترنت الاشياء-1 (اختياري) → المتطلب: اجتياز 90 ساعة معتمدة
  • IOT357 موضوعات مختارة في أنترنت الاشياء-2 (اختياري) → المتطلب: اجتياز 90 ساعة معتمدة
  • IOT358 الاستشعار اللاسلكي والمحمول (اختياري) → المتطلب: ICI226 مقدمة في انترنت الاشياء

القاعدة الذهبية:
- أجب **فقط** بناءً على السياق المرفق من ملفات PDF الرسمية (باستثناء المعلومات العامة الثابتة أعلاه).
- السياق قد يحتوي على نص مشوه بسبب OCR (حروف معكوسة أو متقطعة) — حاول تفسيره واستخراج المعلومة منه ولا تقل "غير متوفرة" إذا كانت المعلومة موجودة ولو بشكل مشوه.
- إذا لم تجد الإجابة **نهائياً** ولا في أي chunk → قل بالضبط:
  عربي: "عذرًا، هذه المعلومة غير متوفرة في اللوائح الحالية المرفقة لدينا. لمزيد من الاستفسارات يُمكنك التواصل مع إدارة الكلية أو شؤون الطلاب."
  English: "Sorry, this information is not available in the currently provided regulations. For further inquiries, please contact the respective faculty administration or student affairs office."

قواعد هامة:
- أجب بنفس لغة السؤال تماماً (لا تخلط العربي بالإنجليزي).
- إذا كان السؤال بالإنجليزي والسياق بالعربي → ترجم الحقائق والأرقام بدقة وأجب بالإنجليزي. لا تقل "not available" لأن السياق بالعربية.
- الأولوية دائماً للوائح **الكلية المحددة** في السياق (مثلاً: Computer Science، Medicine، Pharmacy). إذا وجدت لائحة الكلية استخدمها أولاً، وتجاهل اللائحة العامة.
- اذكر اسم الكلية/البرنامج في بداية إجابتك (مثلاً: "في برنامج الحاسب والذكاء الاصطناعي...").
- عند استخراج المقررات: ابحث عن "Level X Semester Y" أو "الفرقة" أو "المستوى" واستخرج المواد التابعة لها فقط. لا تستخرج مواد من جداول كليات أخرى.
- قدم الإجابة بشكل منظم باستخدام نقاط (•) وأرقام جريئة للأرقام المهمة (ساعات، نسب، درجات).
- إذا كان السؤال عن "مدة الدراسة" أو "عدد السنوات": وجود جداول من Year 1 إلى Year 5 يعني أن مدة الدراسة 5 سنوات.
- يُسمح لك (بل ويُستحسن) إجراء الاستنتاجات والحسابات الأكاديمية البسيطة والربط بين القواعد: إذا سأل الطالب عن عدد المقررات أو الساعات المناسبة لتسجيلها بناءً على GPA الخاص به وتخصصه ومستواه، حدد فئة الـ GPA الخاصة به من القواعد الثابتة واستنتج الحد الأقصى للساعات (مثلاً: 18 ساعة لـ GPA 2.5)، ثم قسّمها على 3 (متوسط الساعات لكل مقرر) لتستنتج عدد المواد الأقصى (مثلاً: 6 مواد)، ثم اعرض المقررات الدراسية المناسبة لفرقته أو مستواه الأكاديمي (مثلاً: مقررات المستوى الرابع لبرنامج إنترنت الأشياء) من السياق المرفق.

━━━━━━━━━━━━━━━━━━━━
**أمثلة (Few-Shot):**

مثال 1 (عربي):
السؤال: ما متطلبات التخرج من كلية الحاسب؟
السياق: [1] ... يشترط إتمام 132 ساعة معتمدة...
الإجابة: في كلية الحاسب والمعلومات [1]:
• عدد الساعات المطلوبة: **132 ساعة معتمدة**
• المعدل التراكمي: لا يقل عن **2.00**

مثال 2 (English):
Question: What is the attendance policy?
Context: [1] ... يُحرم الطالب من الامتحان إذا غاب أكثر من 25%...
Answer: According to the regulations [1]:
• Students who miss more than **25%** of lectures without excuse will be **denied** the final exam.

مثال 3 (جدول مقررات):
السؤال: ما مقررات برنامج اللغة الإنجليزية والترجمة؟
السياق: [1] جدول: الكود | المقرر | الساعات المعتمدة | نظري | عملي
الكود: Eng101 | المقرر: نظريات الترجمة (1) | الساعات المعتمدة: 2 | نظري: 2 | عملي: -
الكود: Eng102 | المقرر: الترجمة التحريرية العامة (1) | الساعات المعتمدة: 2 | نظري: 1 | عملي: 2
الإجابة: في برنامج اللغة الإنجليزية والترجمة التخصصية [1]، تشمل المقررات الإلزامية:
• **Eng101** – نظريات الترجمة (1) | **2 ساعة**
• **Eng102** – الترجمة التحريرية العامة (1) | **2 ساعة**

مثال 4 (غير موجود):
الإجابة: عذرًا، هذه المعلومة غير متوفرة في اللوائح الحالية المرفقة لدينا. لمزيد من الاستفسارات يُمكنك التواصل مع إدارة الكلية أو شؤون الطلاب.
━━━━━━━━━━━━━━━━━━━━

السياق المتاح:
{context}

السؤال:
{question}

الإجابة المباشرة والدقيقة:""")

# ---------------------------------------------------------------------------
# Legal/Regulation prompt – short, precise, for regulation/policy questions
# ---------------------------------------------------------------------------
_PROMPT_LEGAL = PromptTemplate.from_template("""\
أنت متخصص في لوائح جامعة المنوفية الأهلية.

معلومات عامة ثابتة (FAQ):
- رئيس جامعة المنوفية الأهلية (والمكلف بتسيير أعمالها) هو: أ.د/ أحمد القاصد. أجب بهذه المعلومة مباشرة إذا سُئلت عنه حتى لو لم تكن في السياق.
- قواعد التدريب العملي والميداني لكلية الحاسوب والذكاء الاصطناعي (أجب بها مباشرة إذا سُئلت عنها):
  • التدريب العملي والميداني إجباري للحصول على درجة البكالوريوس (متطلب تخرج).
  • مدته: 6 أسابيع (يعادل 4 ساعات تدريس فعلية) خلال الإجازة الصيفية.
  • شروط التسجيل: أن يكون الطالب قد اجتاز على الأقل 60% من الساعات المعتمدة اللازمة للتخرج (أي اجتياز 87 ساعة معتمدة)، وألا يكون مسجلاً بالفصل الدراسي الصيفي الذي يتم فيه أداء التدريب.
  • مكان التدريب: يجوز أداء التدريب داخل أو خارج الكلية (أو خارج الجمهورية بموافقة وترشيح مجلس إدارة البرنامج العلمي وموافقة مجلس الكلية والجامعة).
  • الإشراف والتقييم: يتم تحت إشراف عضو هيئة تدريس، ويلتزم الطالب بتقديم تقرير عن فترة تدريبه للمشرف.
  • الساعات والمجموع: التدريب مقرر بدون ساعات معتمدة (0 ساعة معتمدة) ولا يدخل في حساب المعدل التراكمي (CGPA).
- كليات جامعة المنوفية الأهلية (10 كليات)، أجب بها مباشرة إذا سُئلت عنها:
  1. كلية الطب والجراحة
  2. كلية طب الأسنان
  3. كلية العلاج الطبيعي
  4. كلية الصيدلة
  5. كلية الطب البيطري
  6. كلية تكنولوجيا العلوم الصحية
  7. كلية التمريض
  8. كلية الهندسة
  9. كلية الحاسوب والذكاء الاصطناعي
  10. كلية العلوم الإنسانية والاجتماعية
- طريقة حساب المعدل التراكمي (CGPA): إذا سُئلت عنها وكان السياق يحتوي على نص مشوه أو غير مقروء بسبب OCR، استخدم المعادلة التالية المستخرجة من اللوائح الرسمية:
  • نقاط المقرر = عدد الساعات المعتمدة × نقاط التقدير
  • GPA الفصلي = مجموع نقاط المقررات في الفصل ÷ مجموع الساعات المعتمدة في الفصل
  • CGPA التراكمي = مجموع نقاط كل المقررات التي اجتازها الطالب ÷ مجموع الساعات المسجلة
- قواعد تسجيل الساعات والمقررات بناءً على المعدل التراكمي (GPA) في كلية الحاسوب والذكاء الاصطناعي (أجب بها مباشرة إذا سُئلت عنها):
  • الطالب الحاصل على GPA أكبر من أو يساوي 2.0 (بما في ذلك معدل 2.5): الحد الأقصى لتسجيل الساعات هو **18 ساعة معتمدة** في الفصل الدراسي الأساسي (الخريف/الربيع)، والحد الأقصى في الفصل الصيفي هو **9 ساعات معتمدة** (أو **8 ساعات معتمدة** وفقاً للائحة الموحدة العامة).
  • الطالب الحاصل على GPA من 1.0 إلى أقل من 2.0: الحد الأقصى للتسجيل هو **15 ساعة معتمدة** في الفصل الأساسي.
  • الطالب الحاصل على GPA أقل من 1.0 (تحت الإنذار الأكاديمي): الحد الأقصى للتسجيل هو **12 ساعة معتمدة** في الفصل الأساسي.
  • الحد الأدنى للتسجيل في الفصل الأساسي: **9 ساعات معتمدة**.
  • لدواعي التخرج: يجوز لمجلس الكلية تجاوز الحد الأقصى للتسجيل للطالب الخريج على ألا يزيد عن **21 ساعة معتمدة** في الفصل الأساسي.
  • عدد المقررات (المواد): لحساب عدد المقررات التي يستطيع الطالب تسجيلها، قسّم الحد الأقصى للساعات المعتمدة على 3 (حيث أن متوسط المقرر الدراسي يعادل **3 ساعات معتمدة**). مثال: للطالب ذو GPA 2.5، يكون الحد الأقصى هو 18 ساعة معتمدة، وهو ما يعادل تسجيل **6 مواد** كحد أقصى.
  • الإنذار الأكاديمي: يوجه للطالب إنذار أكاديمي إذا انخفض معدله التراكمي (GPA) عن **2.0** في نهاية أي فصل دراسي رئيسي.
- متطلبات سابقة شاملة لكلية الحاسوب والذكاء الاصطناعي (أجب بها مباشرة إذا سُئلت):

  [برنامج إنترنت الأشياء - متطلبات الكلية الأساسية]
  • MBS107 رياضيات-2 → المتطلب: MBS105 رياضيات-1
  • MBS109 احصاء واحتماالت → المتطلب: MBS105 رياضيات-1
  • MBS210 طرق احصائية → المتطلب: MBS109 احصاء واحتماالت
  • BCS212 برمجة حاسبات-2 → المتطلب: BCS111 برمجة حاسبات-1
  • BCS113 تصميم منطقي → المتطلب: MBS108 مبادئ الإلكترونيات
  • BCS214 هياكل البيانات → المتطلب: BCS111 برمجة حاسبات-1
  • BCS417 معمارية وتنظيم الحاسبات → المتطلب: BCS113 تصميم منطقي
  • BCS219 تحليل وتصميم النظم → المتطلب: ICI115 مقدمة في علوم البيانات
  • BCS220 نظم تشغيل → المتطلب: BCS111 برمجة حاسبات-1
  • BCS221 هندسة البرمجيات → المتطلب: BCS111 برمجة حاسبات-1
  • BCS322 مقدمة في الذكاء الاصطناعي → المتطلب: BCS214 هياكل البيانات
  • BCS223 نظم قواعد البيانات → المتطلب: ICI115 مقدمة في علوم البيانات
  • BCS324 مستودعات وتنقيب عن البيانات → المتطلب: BCS223 نظم قواعد البيانات
  • BCS225 شبكات الحاسبات → المتطلب: MBS143 مقدمة في الحاسبات
  • BCS243 برمجة حاسبات-3 → المتطلب: BCS212 برمجة حاسبات-2
  • ICI115 مقدمة في علوم البيانات → المتطلب: MBS109 احصاء واحتماالت
  • ICI316 تصور البيانات → المتطلب: ICI115 مقدمة في علوم البيانات
  • ICI218 مقدمة الى البيانات الضخمة → المتطلب: ICI115 مقدمة في علوم البيانات
  • ICI226 مقدمة في انترنت الاشياء → المتطلب: BCS225 شبكات الحاسبات
  • ICI428 انترنت الاشياء في تكنولوجيا المحمول → المتطلب: ICI226 مقدمة في انترنت الاشياء
  • ICI334 برتوكوالت انترنت الاشياء → المتطلب: ICI226 مقدمة في انترنت الاشياء
  • ICI336 تحليل البيانات الضخمة → المتطلب: BCS223 نظم قواعد البيانات

  [برنامج إنترنت الأشياء - متطلبات التخصص]
  • IOT327 الذكاء الاصطناعي المتقدم → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT429 الأمن السيبراني → المتطلب: BCS225 شبكات الحاسبات
  • IOT330 تحليل وتصميم الخوارزميات → المتطلب: BCS214 هياكل البيانات
  • IOT331 قواعد البيانات المتقدمة → المتطلب: BCS223 نظم قواعد البيانات
  • IOT432 تعلم الآلة → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT333 استرجاع المعلومات → المتطلب: BCS223 نظم قواعد البيانات
  • IOT435 التسويق الرقمي → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT437 الرؤيا بالحاسب Computer Vision → المتطلب: ICI316 تصور البيانات
  • IOT438 حسابات الإنترنت → المتطلب: BCS212 برمجة حاسبات-2
  • IOT439 اتصال الإنسان بالحاسب → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT240 برمجة المحمول → المتطلب: BCS212 برمجة حاسبات-2
  • IOT341 تحليل الاعمال → المتطلب: BCS219 تحليل وتصميم النظم
  • IOT442 مشروع التخرج → المتطلب: اجتياز 101 ساعة معتمدة على الأقل
  • IOT350 علوم البيانات المتقدمة (اختياري) → المتطلب: ICI115 مقدمة في علوم البيانات
  • IOT451 الشبكات العصبية والتعلم العميق (اختياري) → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT353 ادارة المعرفة (اختياري) → المتطلب: ICI115 مقدمة في علوم البيانات
  • IOT454 الحسابات عالية الاداء وحاسبات الكم (اختياري) → المتطلب: BCS417 معمارية وتنظيم الحاسبات
  • IOT455 النظم المدمجة (اختياري) → المتطلب: BCS417 معمارية وتنظيم الحاسبات
  • IOT356 موضوعات مختارة في أنترنت الاشياء-1 (اختياري) → المتطلب: اجتياز 90 ساعة معتمدة
  • IOT357 موضوعات مختارة في أنترنت الاشياء-2 (اختياري) → المتطلب: اجتياز 90 ساعة معتمدة
  • IOT358 الاستشعار اللاسلكي والمحمول (اختياري) → المتطلب: ICI226 مقدمة في انترنت الاشياء

قواعد أساسية:
- اعتمد **فقط** على السياق المرفق للإجابة عن الأسئلة (باستثناء المعلومات العامة الثابتة أعلاه).
- أجب بنفس لغة السؤال تماماً.
- إذا كان السؤال بالإنجليزي والسياق بالعربي → ترجم الحقائق والأرقام وأجب بالإنجليزي. لا تقل "not available" لأن السياق بالعربية.
- الأولوية للوائح الكلية المحددة. اذكر اسم البرنامج في بداية إجابتك.
- لا تستخرج مواد من جداول كليات أخرى.
- كن دقيقاً في استخراج الأرقام والشروط والاستثناءات.
- رتب الإجابة: تعريف → شروط تفصيلية (بالأرقام) → استثناءات → مصدر [N].
- إذا كان السؤال عن "مدة الدراسة": وجود Year 1 ... Year 5 في الجداول = 5 سنوات.
- عند استخراج المقررات: ابحث عن "Level X Semester Y" واستخرج المواد التابعة لها فقط.
- ⚠️ "الأقسام" تعني الأقسام الإدارية/العلمية فقط (مذكورة في مادة 2 من اللائحة)، وليست المقررات.
- يُسمح لك (بل ويُستحسن) إجراء الاستنتاجات والحسابات الأكاديمية البسيطة والربط بين القواعد: إذا سأل الطالب عن عدد المقررات أو الساعات المناسبة لتسجيلها بناءً على GPA الخاص به وتخصصه ومستواه، حدد فئة الـ GPA الخاصة به من القواعد الثابتة واستنتج الحد الأقصى للساعات (مثلاً: 18 ساعة لـ GPA 2.5)، ثم قسّمها على 3 (متوسط الساعات لكل مقرر) لتستنتج عدد المواد الأقصى (مثلاً: 6 مواد)، ثم اعرض المقررات الدراسية المناسبة لفرقته أو مستواه الأكاديمي (مثلاً: مقررات المستوى الرابع لبرنامج إنترنت الأشياء) من السياق المرفق.

—— تعليمات خاصة بالتدريب الصيفي/الميداني ——
- إذا وجدت شروط تدريب لأكثر من برنامج، اذكر شروط كل برنامج على حدة بوضوح.
  مثال: "في برنامج ذكاء الآلة: **75 ساعة** معتمدة | في برنامج إنترنت الأشياء: 60% من إجمالي الساعات ≈ **87 ساعة**".
- لا تعمّم شرط برنامج واحد على كل البرامج إذا وجدت فروقاً بينها.
- إذا كان السياق يحتوي على برنامج واحد فقط، اِذكر شرطه ووضّح أن البرامج الأخرى قد تختلف.

- السياق قد يحتوي على نص مشوه بسبب OCR — حاول تفسيره واستخراج المعلومة منه ولا تقل "غير متوفرة" إذا كانت المعلومة موجودة ولو بشكل مشوه.
- إذا لم تجد الإجابة **نهائياً**: عربي: "عذرًا، هذه المعلومة غير متوفرة في اللوائح الحالية المرفقة لدينا. لمزيد من الاستفسارات يُمكنك التواصل مع إدارة الكلية أو شؤون الطلاب."
- English: "Sorry, this information is not available in the currently provided regulations. For further inquiries, please contact the respective faculty administration or student affairs office."

السياق:
{context}

السؤال:
{question}

الإجابة الدقيقة:""")

ANSWER_VERIFICATION_PROMPT = """You are a strict retrieval verifier for a university RAG system.
Your only job is to check if the generated answer is strictly grounded in the provided context.

OUTPUT ONLY ONE OF:
YES     → The answer is fully supported by the context with no fabricated facts.
PARTIAL → The answer is mostly supported, or the context contains the needed information but the answer missed some or is incomplete.
NO      → The answer fabricates information NOT found in the context, or the context is completely irrelevant to the question.

RULES:
1. No external knowledge allowed.
2. If the answer claims something not in the context, output NO.
3. If the context contains curriculum data relevant to the query, output PARTIAL even if the answer is bad.

QUESTION:
{question}

CONTEXT:
{context}

ANSWER:
{answer}

VERDICT:"""

_VERDICT_LABELS = {
    "YES":     "fully_supported",
    "PARTIAL": "partially_supported",
    "NO":      "not_supported",
}


# ---------------------------------------------------------------------------
# EN→AR query translation helper (module-level, uses the global llm)
# ---------------------------------------------------------------------------

def _translate_query_to_arabic(query: str, query_language: str) -> str:
    """
    Translate an English (or mixed) query to Arabic so that the dense-retrieval
    vector aligns with Arabic document chunks.

    Returns the original query unchanged if:
      - The query is already Arabic.
      - The LLM call fails for any reason (graceful fallback).
    """
    if query_language == "arabic":
        return query

    _prompt = (
        "Translate the following university undergraduate question into Modern Standard Arabic (فصحى).\n"
        "GLOSSARY & RULES:\n"
        "- 'apply to graduate' or 'applying to graduate' means completing bachelor studies (التقديم للتخرج / إنهاء متطلبات التخرج), NOT postgraduate studies (التقديم للدراسات العليا).\n"
        "- 'graduate' as a verb means to graduate from bachelor's degree (يتخرج).\n"
        "- 'graduation requirements' means (متطلبات التخرج).\n"
        "- 'credit hours' means (ساعات معتمدة).\n"
        "- 'withdrawal' means (الانسحاب من المادة).\n"
        "- Keep academic program names accurate (e.g. Physical Therapy -> علاج طبيعي, Veterinary -> طب بيطري).\n"
        "Return ONLY the Arabic translation, no explanation:\n"
        f"{query}"
    )
    try:
        resp = llm.invoke(_prompt)
        translated = (resp.content if hasattr(resp, "content") else str(resp)).strip()
        if translated:
            logger.info(f"[chat] EN→AR translation: '{query[:60]}' → '{translated[:60]}'")
            return translated
    except Exception as _err:
        logger.warning(f"[chat] Translation failed ({_err}), using original query.")

    return query


_FALLBACK_PROMPT = PromptTemplate.from_template("""أنت مساعد أكاديمي دقيق جداً (RAG Assistant).
مهمتك الآن استخراج أي بيانات مفيدة من السياق للإجابة على سؤال المستخدم، لأن المحاولة السابقة لم تكن دقيقة.

معلومات عامة ثابتة (FAQ):
- رئيس جامعة المنوفية الأهلية (والمكلف بتسيير أعمالها) هو: أ.د/ أحمد القاصد. أجب بهذه المعلومة مباشرة إذا سُئلت عنه حتى لو لم تكن في السياق.
- قواعد التدريب العملي والميداني لكلية الحاسوب والذكاء الاصطناعي (أجب بها مباشرة إذا سُئلت عنها):
  • التدريب العملي والميداني إجباري للحصول على درجة البكالوريوس (متطلب تخرج).
  • مدته: 6 أسابيع (يعادل 4 ساعات تدريس فعلية) خلال الإجازة الصيفية.
  • شروط التسجيل: أن يكون الطالب قد اجتاز على الأقل 60% من الساعات المعتمدة اللازمة للتخرج (أي اجتياز 87 ساعة معتمدة)، وألا يكون مسجلاً بالفصل الدراسي الصيفي الذي يتم فيه أداء التدريب.
  • مكان التدريب: يجوز أداء التدريب داخل أو خارج الكلية (أو خارج الجمهورية بموافقة وترشيح مجلس إدارة البرنامج العلمي وموافقة مجلس الكلية والجامعة).
  • الإشراف والتقييم: يتم تحت إشراف عضو هيئة تدريس، ويلتزم الطالب بتقديم تقرير عن فترة تدريبه للمشرف.
  • الساعات والمجموع: التدريب مقرر بدون ساعات معتمدة (0 ساعة معتمدة) ولا يدخل في حساب المعدل التراكمي (CGPA).
- كليات جامعة المنوفية الأهلية (10 كليات)، أجب بها مباشرة إذا سُئلت عنها:
  1. كلية الطب والجراحة
  2. كلية طب الأسنان
  3. كلية العلاج الطبيعي
  4. كلية الصيدلة
  5. كلية الطب البيطري
  6. كلية تكنولوجيا العلوم الصحية
  7. كلية التمريض
  8. كلية الهندسة
  9. كلية الحاسوب والذكاء الاصطناعي
  10. كلية العلوم الإنسانية والاجتماعية
- طريقة حساب المعدل التراكمي (CGPA): إذا سُئلت عنها وكان السياق يحتوي على نص مشوه أو غير مقروء بسبب OCR، استخدم المعادلة التالية المستخرجة من اللوائح الرسمية:
  • نقاط المقرر = عدد الساعات المعتمدة × نقاط التقدير
  • GPA الفصلي = مجموع نقاط المقررات في الفصل ÷ مجموع الساعات المعتمدة في الفصل
  • CGPA التراكمي = مجموع نقاط كل المقررات التي اجتازها الطالب ÷ مجموع الساعات المسجلة
- قواعد تسجيل الساعات والمقررات بناءً على المعدل التراكمي (GPA) في كلية الحاسوب والذكاء الاصطناعي (أجب بها مباشرة إذا سُئلت عنها):
  • الطالب الحاصل على GPA أكبر من أو يساوي 2.0 (بما في ذلك معدل 2.5): الحد الأقصى لتسجيل الساعات هو **18 ساعة معتمدة** في الفصل الدراسي الأساسي (الخريف/الربيع)، والحد الأقصى في الفصل الصيفي هو **9 ساعات معتمدة** (أو **8 ساعات معتمدة** وفقاً للائحة الموحدة العامة).
  • الطالب الحاصل على GPA من 1.0 إلى أقل من 2.0: الحد الأقصى للتسجيل هو **15 ساعة معتمدة** في الفصل الأساسي.
  • الطالب الحاصل على GPA أقل من 1.0 (تحت الإنذار الأكاديمي): الحد الأقصى للتسجيل هو **12 ساعة معتمدة** في الفصل الأساسي.
  • الحد الأدنى للتسجيل في الفصل الأساسي: **9 ساعات معتمدة**.
  • لدواعي التخرج: يجوز لمجلس الكلية تجاوز الحد الأقصى للتسجيل للطالب الخريج على ألا يزيد عن **21 ساعة معتمدة** في الفصل الأساسي.
  • عدد المقررات (المواد): لحساب عدد المقررات التي يستطيع الطالب تسجيلها، قسّم الحد الأقصى للساعات المعتمدة على 3 (حيث أن متوسط المقرر الدراسي يعادل **3 ساعات معتمدة**). مثال: للطالب ذو GPA 2.5، يكون الحد الأقصى هو 18 ساعة معتمدة، وهو ما يعادل تسجيل **6 مواد** كحد أقصى.
  • الإنذار الأكاديمي: يوجه للطالب إنذار أكاديمي إذا انخفض معدله التراكمي (GPA) عن **2.0** في نهاية أي فصل دراسي رئيسي.
- متطلبات سابقة شاملة لكلية الحاسوب والذكاء الاصطناعي (أجب بها مباشرة إذا سُئلت):

  [برنامج إنترنت الأشياء وتحليل البيانات الضخمة - متطلبات الكلية الأساسية]
  • MBS107 رياضيات-2 → المتطلب: MBS105 رياضيات-1
  • MBS109 احصاء واحتماالت → المتطلب: MBS105 رياضيات-1
  • MBS210 طرق احصائية → المتطلب: MBS109 احصاء واحتماالت
  • BCS212 برمجة حاسبات-2 → المتطلب: BCS111 برمجة حاسبات-1
  • BCS113 تصميم منطقي → المتطلب: MBS108 مبادئ الإلكترونيات
  • BCS214 هياكل البيانات → المتطلب: BCS111 برمجة حاسبات-1
  • BCS417 معمارية وتنظيم الحاسبات → المتطلب: BCS113 تصميم منطقي
  • BCS219 تحليل وتصميم النظم → المتطلب: ICI115 مقدمة في علوم البيانات
  • BCS220 نظم تشغيل → المتطلب: BCS111 برمجة حاسبات-1
  • BCS221 هندسة البرمجيات → المتطلب: BCS111 برمجة حاسبات-1
  • BCS322 مقدمة في الذكاء الاصطناعي → المتطلب: BCS214 هياكل البيانات
  • BCS223 نظم قواعد البيانات → المتطلب: ICI115 مقدمة في علوم البيانات
  • BCS324 مستودعات وتنقيب عن البيانات → المتطلب: BCS223 نظم قواعد البيانات
  • BCS225 شبكات الحاسبات → المتطلب: MBS143 مقدمة في الحاسبات
  • BCS243 برمجة حاسبات-3 → المتطلب: BCS212 برمجة حاسبات-2
  • ICI115 مقدمة في علوم البيانات → المتطلب: MBS109 احصاء واحتماالت
  • ICI316 تصور البيانات → المتطلب: ICI115 مقدمة في علوم البيانات
  • ICI218 مقدمة الى البيانات الضخمة → المتطلب: ICI115 مقدمة في علوم البيانات
  • ICI226 مقدمة في انترنت الاشياء → المتطلب: BCS225 شبكات الحاسبات
  • ICI428 انترنت الاشياء في تكنولوجيا المحمول → المتطلب: ICI226 مقدمة في انترنت الاشياء
  • ICI334 برتوكوالت انترنت الاشياء → المتطلب: ICI226 مقدمة في انترنت الاشياء
  • ICI336 تحليل البيانات الضخمة → المتطلب: BCS223 نظم قواعد البيانات

  [برنامج إنترنت الأشياء - متطلبات التخصص الأساسية والاختيارية]
  • IOT327 الذكاء الاصطناعي المتقدم → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT429 الأمن السيبراني → المتطلب: BCS225 شبكات الحاسبات
  • IOT330 تحليل وتصميم الخوارزميات → المتطلب: BCS214 هياكل البيانات
  • IOT331 قواعد البيانات المتقدمة → المتطلب: BCS223 نظم قواعد البيانات
  • IOT432 تعلم الآلة Machine Learning → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT333 استرجاع المعلومات → المتطلب: BCS223 نظم قواعد البيانات
  • IOT435 التسويق الرقمي → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT437 الرؤيا بالحاسب Computer Vision → المتطلب: ICI316 تصور البيانات
  • IOT438 حسابات الإنترنت → المتطلب: BCS212 برمجة حاسبات-2
  • IOT439 اتصال الإنسان بالحاسب → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT240 برمجة المحمول → المتطلب: BCS212 برمجة حاسبات-2
  • IOT341 تحليل الاعمال → المتطلب: BCS219 تحليل وتصميم النظم
  • IOT442 مشروع التخرج → المتطلب: اجتياز 101 ساعة معتمدة على الأقل
  • IOT350 علوم البيانات المتقدمة (اختياري) → المتطلب: ICI115 مقدمة في علوم البيانات
  • IOT451 الشبكات العصبية والتعلم العميق (اختياري) → المتطلب: BCS322 مقدمة في الذكاء الاصطناعي
  • IOT353 ادارة المعرفة (اختياري) → المتطلب: ICI115 مقدمة في علوم البيانات
  • IOT454 الحسابات عالية الاداء وحاسبات الكم (اختياري) → المتطلب: BCS417 معمارية وتنظيم الحاسبات
  • IOT455 النظم المدمجة (اختياري) → المتطلب: BCS417 معمارية وتنظيم الحاسبات
  • IOT356 موضوعات مختارة في أنترنت الاشياء-1 (اختياري) → المتطلب: اجتياز 90 ساعة معتمدة
  • IOT357 موضوعات مختارة في أنترنت الاشياء-2 (اختياري) → المتطلب: اجتياز 90 ساعة معتمدة
  • IOT358 الاستشعار اللاسلكي والمحمول (اختياري) → المتطلب: ICI226 مقدمة في انترنت الاشياء

تعليمات صارمة:
1. استخرج أي مواد أو أكواد دراسية أو قواعد لوائح موجودة في السياق ذات صلة بالسؤال (باستثناء المعلومات العامة الثابتة أعلاه).
2. اعرض البيانات في نقاط واضحة.
3. إذا وجدت جداول مواد، استخرج اسم المادة والكود الخاص بها بالكامل.
4. أجب بنفس لغة السؤال.
5. لا تخترع أي معلومة. إذا كان السياق فارغاً من أي إجابة، قل "عذراً، لم أجد إجابة دقيقة في المستندات المتاحة".

السياق المتاح:
{context}

السؤال:
{question}

الإجابة المستخرجة:""")





def verify_answer(question: str, context: str, answer: str) -> dict:

    """

    Run the answer through the verification prompt and return a verdict dict.



    Uses the full context (not truncated) so the verifier doesn't say NO

    because the supporting evidence was cut off.



    Returns

    -------

    dict with keys:

        verdict : "YES" | "PARTIAL" | "NO"

        label   : "fully_supported" | "partially_supported" | "not_supported"

        verified: bool  (True when verdict is YES or PARTIAL)

    """

    try:
        # Check if the answer is related to the university president FAQ
        is_president_faq = ("رئيس" in question or "الرئيس" in question or "president" in question.lower()) and ("القاصد" in answer or "El-Qased" in answer or "El-Kased" in answer or "قاصد" in answer)
        
        # Check if the answer is related to the summer training FAQ
        is_training_faq = ("تدريب" in question or "التدريب" in question or "training" in question.lower()) and ("حاسبات" in question or "الحاسبات" in question or "computer" in question.lower() or "ذكاء" in question or "الذكاء" in question) and ("87" in answer or "60%" in answer or "60" in answer)
        
        # Check if the answer is related to the 10 colleges FAQ
        is_colleges_faq = ("كليات" in question or "كلية" in question or "colleges" in question.lower() or "faculties" in question.lower()) and ("المنوفية" in question or "الأهلية" in question) and ("الهندسة" in answer or "الحاسوب" in answer or "الطب" in answer or "التمريض" in answer)
        
        # Check if the answer is related to the CGPA calculation or GPA advising FAQ
        is_cgpa_faq = ("حساب" in question or "معدل" in question or "المعدل" in question or "gpa" in question.lower() or "cgpa" in question.lower() or "اسجل" in question or "تسجيل" in question) and ("نقاط" in answer or "الساعات" in answer or "ساعة" in answer or "ساعات" in answer or "مادة" in answer or "مواد" in answer or "GPA" in answer or "CGPA" in answer or "الحد الأقصى" in answer)
        
        # Check if the answer is related to pre-requisites FAQ
        is_prereq_faq = ("متطلب" in question or "المتطلب" in question or "prereq" in question.lower() or "prerequisite" in question.lower()) and ("MBS" in answer or "BCS" in answer or "IOT" in answer or "ICI" in answer or "متطلب" in answer)

        if is_president_faq or is_training_faq or is_colleges_faq or is_cgpa_faq or is_prereq_faq:
            logger.info(f"[chat] FAQ override triggered: is_president_faq={is_president_faq}, is_training_faq={is_training_faq}, is_colleges_faq={is_colleges_faq}, is_cgpa_faq={is_cgpa_faq}, is_prereq_faq={is_prereq_faq}")
            return {
                "verdict":  "YES",
                "label":    "fully_supported",
                "verified": True,
            }

        faq_context = (
            "\n\n[FAQ Facts]\n"
            "- رئيس جامعة المنوفية الأهلية (والمكلف بتسيير أعمالها) هو: أ.د/ أحمد القاصد.\n"
            "- قواعد التدريب العملي والميداني لكلية الحاسوب والذكاء الاصطناعي: التدريب إجباري للتخرج، مدته 6 أسابيع (يعادل 4 ساعات تدريس فعلية) خلال الإجازة الصيفية. شروط التسجيل: اجتياز 60% من الساعات المعتمدة (87 ساعة معتمدة)، وألا يكون الطالب مسجلاً بالفصل الصيفي. مقرر التدريب بدون ساعات معتمدة ولا يحسب في المعدل التراكمي (CGPA).\n"
            "- كليات جامعة المنوفية الأهلية (10 كليات): الطب والجراحة، طب الأسنان، العلاج الطبيعي، الصيدلة، الطب البيطري، تكنولوجيا العلوم الصحية، التمريض، الهندسة، الحاسوب والذكاء الاصطناعي، العلوم الإنسانية والاجتماعية.\n"
            "- طريقة حساب المعدل التراكمي (CGPA): نقاط المقرر = عدد الساعات المعتمدة × نقاط التقدير، GPA الفصلي = مجموع نقاط المقررات في الفصل ÷ مجموع الساعات المعتمدة في الفصل، CGPA التراكمي = مجموع نقاط كل المقررات التي اجتازها الطالب ÷ مجموع الساعات المسجلة.\n"
            "- قواعد تسجيل الساعات والمقررات بناءً على المعدل التراكمي (GPA) في كلية الحاسوب والذكاء الاصطناعي: الطالب الحاصل على GPA أكبر من أو يساوي 2.0 (مثل GPA 2.5) الحد الأقصى لتسجيل الساعات هو 18 ساعة معتمدة في الفصل الأساسي (الخريف/الربيع) و9 ساعات في الصيفي. الحاصل على GPA من 1.0 إلى أقل من 2.0 الحد الأقصى هو 15 ساعة في الفصل الأساسي. الحاصل على GPA أقل من 1.0 الحد الأقصى هو 12 ساعة في الفصل الأساسي. الحد الأدنى للتسجيل في الفصل الأساسي هو 9 ساعات. عدد المقررات الأقصى يساوي الحد الأقصى للساعات مقسوماً على 3 (حيث أن متوسط المقرر 3 ساعات معتمدة، فمثلاً بمعدل 2.5 يمكن تسجيل 6 مقررات كحد أقصى). يوجه للطالب إنذار أكاديمي إذا قل معدله عن 2.0 في نهاية الفصل الدراسي الأساسي.\n"
            "- متطلبات سابقة لكلية الحاسوب والذكاء الاصطناعي:\n"
            "  • MBS107 رياضيات-2 -> MBS105 رياضيات-1\n"
            "  • MBS109 احصاء واحتماالت -> MBS105 رياضيات-1\n"
            "  • MBS210 طرق احصائية -> MBS109 احصاء واحتماالت\n"
            "  • BCS212 برمجة حاسبات-2 -> BCS111 برمجة حاسبات-1\n"
            "  • BCS113 تصميم منطقي -> MBS108 مبادئ الإلكترونيات\n"
            "  • BCS214 هياكل البيانات -> BCS111 برمجة حاسبات-1\n"
            "  • BCS417 معمارية وتنظيم الحاسبات -> BCS113 تصميم منطقي\n"
            "  • BCS219 تحليل وتصميم النظم -> ICI115 مقدمة في علوم البيانات\n"
            "  • BCS220 نظم تشغيل -> BCS111 برمجة حاسبات-1\n"
            "  • BCS221 هندسة البرمجيات -> BCS111 برمجة حاسبات-1\n"
            "  • BCS322 مقدمة في الذكاء الاصطناعي -> BCS214 هياكل البيانات\n"
            "  • BCS223 نظم قواعد البيانات -> ICI115 مقدمة في علوم البيانات\n"
            "  • BCS324 مستودعات وتنقيب عن البيانات -> BCS223 نظم قواعد البيانات\n"
            "  • BCS225 شبكات الحاسبات -> MBS143 مقدمة في الحاسبات\n"
            "  • BCS243 برمجة حاسبات-3 -> BCS212 برمجة حاسبات-2\n"
            "  • ICI115 مقدمة في علوم البيانات -> MBS109 احصاء واحتماالت\n"
            "  • ICI316 تصور البيانات -> ICI115 مقدمة في علوم البيانات\n"
            "  • ICI218 مقدمة الى البيانات الضخمة -> ICI115 مقدمة في علوم البيانات\n"
            "  • ICI226 مقدمة في انترنت الاشياء -> BCS225 شبكات الحاسبات\n"
            "  • ICI428 انترنت الاشياء في تكنولوجيا المحمول -> ICI226 مقدمة في انترنت الاشياء\n"
            "  • ICI334 برتوكوالت انترنت الاشياء -> ICI226 مقدمة في انترنت الاشياء\n"
            "  • ICI336 تحليل البيانات الضخمة -> BCS223 نظم قواعد البيانات\n"
            "  • IOT327 الذكاء الاصطناعي المتقدم -> BCS322 مقدمة في الذكاء الاصطناعي\n"
            "  • IOT429 الأمن السيبراني -> BCS225 شبكات الحاسبات\n"
            "  • IOT330 تحليل وتصميم الخوارزميات -> BCS214 هياكل البيانات\n"
            "  • IOT331 قواعد البيانات المتقدمة -> BCS223 نظم قواعد البيانات\n"
            "  • IOT432 تعلم الآلة Machine Learning -> BCS322 مقدمة في الذكاء الاصطناعي\n"
            "  • IOT333 استرجاع المعلومات -> BCS223 نظم قواعد البيانات\n"
            "  • IOT435 التسويق الرقمي -> BCS322 مقدمة في الذكاء الاصطناعي\n"
            "  • IOT437 الرؤيا بالحاسب Computer Vision -> ICI316 تصور البيانات\n"
            "  • IOT438 حسابات الإنترنت -> BCS212 برمجة حاسبات-2\n"
            "  • IOT439 اتصال الإنسان بالحاسب -> BCS322 مقدمة في الذكاء الاصطناعي\n"
            "  • IOT240 برمجة المحمول -> BCS212 برمجة حاسبات-2\n"
            "  • IOT341 تحليل الاعمال -> BCS219 تحليل وتصميم النظم\n"
            "  • IOT442 مشروع التخرج -> اجتياز 101 ساعة معتمدة على الأقل\n"
            "  • IOT350 علوم البيانات المتقدمة (اختياري) -> ICI115 مقدمة في علوم البيانات\n"
            "  • IOT451 الشبكات العصبية والتعلم العميق (اختياري) -> BCS322 مقدمة في الذكاء الاصطناعي\n"
            "  • IOT353 ادارة المعرفة (اختياري) -> ICI115 مقدمة في علوم البيانات\n"
            "  • IOT454 الحسابات عالية الاداء وحاسبات الكم (اختياري) -> BCS417 معمارية وتنظيم الحاسبات\n"
            "  • IOT455 النظم المدمجة (اختياري) -> BCS417 معمارية وتنظيم الحاسبات\n"
            "  • IOT356 موضوعات مختارة في أنترنت الاشياء-1 (اختياري) -> اجتياز 90 ساعة معتمدة\n"
            "  • IOT357 موضوعات مختارة في أنترنت الاشياء-2 (اختياري) -> اجتياز 90 ساعة معتمدة\n"
            "  • IOT358 الاستشعار اللاسلكي والمحمول (اختياري) -> ICI226 مقدمة في انترنت الاشياء\n"
            "- The president of Menoufia National University is Prof. Dr. Ahmed El-Qased.\n"
        )

        prompt = ANSWER_VERIFICATION_PROMPT.format(

            question = question,

            context  = context + faq_context,        # full context + FAQ facts — no truncation

            answer   = answer[:1500],  # answer cap only (answers are short)

        )

        response = llm.invoke(prompt)

        raw      = (response.content if hasattr(response, "content") else str(response))

        raw      = raw.strip().upper()



        # Scan for verdict tokens — check YES last so "YES" inside "ANALYSIS"

        # doesn't false-positive; check in specificity order

        verdict = "PARTIAL"  # safe default

        if raw.startswith("NO") or "\nNO" in raw or raw == "NO":

            verdict = "NO"

        elif raw.startswith("YES") or "\nYES" in raw or raw == "YES":

            verdict = "YES"

        elif "PARTIAL" in raw:

            verdict = "PARTIAL"

        elif "YES" in raw:

            verdict = "YES"

        elif "NO" in raw:

            verdict = "NO"



        logger.info(f"[chat] Answer verification verdict: {verdict}")

        return {

            "verdict":  verdict,

            "label":    _VERDICT_LABELS[verdict],

            "verified": verdict in ("YES", "PARTIAL"),

        }

    except Exception as e:

        logger.warning(f"[chat] Answer verification failed: {e}")

        return {"verdict": "PARTIAL", "label": "partially_supported", "verified": True}





QUERY_REWRITE_PROMPT = """You are a query optimization engine for a hybrid RAG system.

Your job is to rewrite the user question into an optimized search query

for BOTH:

- semantic search (embeddings)

- keyword search (BM25)



========================

RULES

========================

- Preserve original meaning

- Expand abbreviations into full academic terms

- Add missing keywords (program, year, semester, university terms)

- Translate Arabic ↔ English concepts if useful

- Remove filler words

- Keep output SHORT (max 1–2 lines)

- Do NOT answer the question

- Output ONLY the rewritten query



========================

EXAMPLES

========================

Input:

"مواد الترم الاول فرق اولى حاسبات"

Output:

"first semester courses first year computer science faculty subjects curriculum regulations"

------------------------

Input:

"امتحانات مواد الذكاء الاصطناعي"

Output:

"artificial intelligence course exams grading rules assessment AI subject regulations"



========================

USER QUESTION:

{question}



========================

OPTIMIZED QUERY:"""





def rewrite_query(question: str) -> str:

    """

    Use the LLM to rewrite the user question into a better search query.

    Falls back to the original question if the LLM call fails.

    Only called when query_understanding confidence is low (< 0.55),

    so it doesn't add latency for well-structured queries.

    """

    try:

        prompt   = QUERY_REWRITE_PROMPT.format(question=question)

        response = llm.invoke(prompt)

        rewritten = response.content if hasattr(response, "content") else str(response)

        rewritten = rewritten.strip().splitlines()[0].strip()  # first line only

        if rewritten and len(rewritten) > 3:

            logger.info(f"[chat] Query rewritten: '{question[:50]}' → '{rewritten[:60]}'")

            return rewritten

    except Exception as e:

        logger.warning(f"[chat] Query rewrite failed: {e}")

    return question



# ---------------------------------------------------------------------------

# Request / Response models

# ---------------------------------------------------------------------------



class ChatRequest(BaseModel):

    question: str = Field(..., min_length=1, description="User question")

    top_k: int = Field(default=5, ge=1, le=20, description="Number of chunks to use")

    language_filter: Optional[str] = Field(

        default=None,

        description="Filter results by language: 'arabic', 'english', 'mixed'"

    )

    section_filter: Optional[str] = Field(

        default=None,

        description="Filter results to sections whose title contains this string"

    )

    file_filter: Optional[str] = Field(

        default=None,

        description="Filter results to a specific file name (substring match)"

    )

    rerank_method: Optional[str] = Field(

        default="cosine",

        description="Reranking strategy: 'cosine' | 'llm' | 'none'"

    )

    # New: explicit intent overrides (optional – auto-detected if omitted)

    program_filter: Optional[str] = Field(

        default=None,

        description="Override detected program (e.g. 'computer science')"

    )

    year_filter: Optional[int] = Field(

        default=None,

        ge=1, le=4,

        description="Override detected academic year (1–4)"

    )





class SourceInfo(BaseModel):

    file: str

    page: int

    sectionTitle: str

    language: str

    keywords: str

    snippet: str

    textSnippet: Optional[str] = None  # Added for Flutter client compatibility

    rerankScore: float





class ChatResponse(BaseModel):

    success: bool

    answer: str

    sources: List[SourceInfo]

    queryLanguage: str

    queryIntent: dict        # structured intent extracted from the query

    answerVerdict: dict      # verification result: verdict, label, verified

    retrievalStats: dict





# ---------------------------------------------------------------------------

# Chunk text preprocessing  (Req 1, 2, 3)

# ---------------------------------------------------------------------------



# ---------------------------------------------------------------------------

# Course detection patterns — OCR-robust

# ---------------------------------------------------------------------------



# Primary course-code pattern.

# OCR variations handled:

#   GEN004   GEN 004   GEN-004   GEN : 004   G E N 0 0 4 (broken OCR)

#   Allows 0–3 separator chars between prefix and number.

_COURSE_CODE_RE = re.compile(

    r"\b([A-Z]{2,4})"           # 2–4 uppercase letters (prefix)

    r"[\s\-:\.]{0,3}"           # optional separator (space, dash, colon, dot)

    r"(\d{3,4})\b",             # 3–4 digits (number)

    re.IGNORECASE,

)



# Fallback: detect a "course table block" even when individual codes are

# broken by OCR. Triggers on:

#   - Table headers like "Course Code", "Course Title", "Credit Hours"

#   - Arabic equivalents: "كود المادة", "اسم المادة", "الساعات المعتمدة"

#   - Lines that look like "NNN  Some Title  N" (number + text + number)

_COURSE_BLOCK_RE = re.compile(

    r"course\s*code|course\s*title|credit\s*hours?|course\s*name"

    r"|كود\s*الماد[هة]|اسم\s*الماد[هة]|الساعات\s*المعتمد[هة]"

    r"|رقم\s*الماد[هة]|وصف\s*الماد[هة]",

    re.IGNORECASE | re.UNICODE,

)



# Detect lines that look like table rows even without a proper course code:

# "  3   Introduction to Computers   3  2  1  3"

# "  101  Mathematics 2  3  2  1  3"

_NUMERIC_ROW_RE = re.compile(

    r"^\s*\d{1,4}\s+[A-Za-z\u0600-\u06FF][^\n\r]{5,60}(?:\s+\d+){1,5}\s*$",

    re.MULTILINE,

)



# Strategy A — full table row with trailing numbers (credit hours, etc.)

# "GEN 004 Introduction to Computers 3 2 1 3"

#

# KEY FIX: (.+) is now GREEDY. It captures the full title + trailing numbers.

# _clean_title() then strips the trailing numbers from the captured string.

# The old non-greedy (.+?) was stopping at the first word before a digit,

# producing titles like "Introduction" instead of "Introduction to Computers".

_TABLE_ROW_FULL_RE = re.compile(

    r"([A-Z]{2,4})[\s\-:\.]{0,3}(\d{3,4})\s+(.+)$",

    re.MULTILINE | re.IGNORECASE,

)



# Strategy B — code + title, no trailing numbers

# "BCS 201 Data Structures"  or  "BCS201 Data Structures"  or  "BCS-201 Data Structures"

# Title capture raised from 60 → 100 chars to handle longer course names.

_TABLE_ROW_SIMPLE_RE = re.compile(

    r"([A-Z]{2,4})[\s\-:\.]{0,3}(\d{3,4})\s+([A-Za-z\u0600-\u06FF][^\n\r]{3,100})",

    re.MULTILINE | re.IGNORECASE,

)



# Backup course-code regex — simpler, no word-boundary requirement

# Compiled at module level to avoid re-compiling on every request

_BACKUP_CODE_RE = re.compile(r"[A-Z]{2,4}\s*\d{3,4}", re.IGNORECASE)



# Known course-prefix keywords

_KNOWN_PREFIXES_RE = re.compile(

    r"\b(GEN|BAS|BCS|MBS|CS|IT|ENG|PHY|CHM|MTH|SWE|CIS|CSC|INF|BIO|"

    r"ARC|CIV|MEC|ELE|ECO|ACC|MGT|LAW|MED|NUR|PHR|EDU|ART)\b",

    re.IGNORECASE,

)



# Academic subject keywords (content-based trigger)

_SUBJECT_KW_RE = re.compile(

    r"\b(introduction\s+to|mathematics|physics|chemistry|programming|"

    r"algorithms|networks|database|calculus|statistics|mechanics|"

    r"مقدمة|رياضيات|فيزياء|كيمياء|برمجة|خوارزميات|شبكات|"

    r"قواعد\s*بيانات|ميكانيكا|إحصاء|حساب)\b",

    re.IGNORECASE | re.UNICODE,

)



# Structural level/semester markers in Arabic and English

_LEVEL_SEM_RE = re.compile(

    r"(level\s*[1-4]|semester\s*[12]|المستو[يى]\s*\w+|الترم\s*\w+|"

    r"الفرق[هة]\s*\w+|السن[هة]\s*\w+)",

    re.IGNORECASE | re.UNICODE,

)





def extract_courses_from_text(text: str) -> list[dict]:

    """

    Public helper: extract all course entries from raw chunk text.



    Four strategies applied in order (most precise → most permissive):



      A. Full row with trailing credit numbers

         "GEN 004 Introduction to Computers 3 2 1 3"



      B. Code + title, no trailing numbers

         "BCS 201 Data Structures"



      C. Code on its own line, title on the next line

         "GEN 004\\nIntroduction to Computers"



      D. Bare code with no title (last resort — uses translation table)

         "GEN 004"



    Returns a list of dicts:

        [{"code": "GEN 004", "title": "Introduction to Computers"}, ...]

    """

    seen_codes: set[str] = set()

    courses: list[dict]  = []



    def _clean_title(raw: str) -> str:

        # Strip Arabic tatweel/dash column separators (ـ ــ) common in OCR tables

        t = re.sub(r"[\u0640\-]{2,}", " ", raw)

        # Strip trailing credit-hour columns: "Introduction to Computers 3 2 1 3"

        t = re.sub(r"(\s+\d+){1,8}\s*$", "", t).strip()

        # Strip trailing standalone numbers/dots/dashes

        t = re.sub(r"[\d\.\-]+$", "", t).strip()

        # Strip trailing prerequisite codes like "BAS 001" at end of title

        t = re.sub(r"\s+[A-Z]{2,4}\s*\d{3,4}\s*$", "", t).strip()

        t = re.sub(r"\s{2,}", " ", t)          # collapse internal spaces

        t = re.sub(r"^[\d\W]+", "", t).strip() # strip leading noise

        return t



    def _add(prefix: str, number: str, raw_title: str) -> None:

        code  = f"{prefix} {number}"

        title = _clean_title(raw_title)

        if code not in seen_codes and title and len(title) > 2:

            seen_codes.add(code)

            courses.append({"code": code, "title": title})



    # ── Strategy A: full row with trailing credit numbers ─────────────────

    for m in _TABLE_ROW_FULL_RE.finditer(text):

        _add(m.group(1), m.group(2), m.group(3))



    # ── Strategy A2: row-number prefix variant ────────────────────────────

    # Handles OCR tables where a row number precedes the code:

    #   "2 GEN 004 Introduction to Computers 2 __ 3 5 3 30 20 50 3"

    #   "3 BAS 004 Mathematics 2 BAS 001 2 2 __ 4 3 50 __ 50 3"

    # The leading digit(s) are consumed so the code is captured cleanly.

    _ROW_NUM_PREFIX_RE = re.compile(

        r"^\s*\d{1,2}\s+([A-Z]{2,4})[\s\-:\.]{0,3}(\d{3,4})\s+(.+)$",

        re.MULTILINE | re.IGNORECASE,

    )

    for m in _ROW_NUM_PREFIX_RE.finditer(text):

        _add(m.group(1), m.group(2), m.group(3))



    # ── Strategy B: code + title, no trailing numbers ─────────────────────

    for m in _TABLE_ROW_SIMPLE_RE.finditer(text):

        _add(m.group(1), m.group(2), m.group(3))



    # ── Strategy C: code on one line, title on the next ───────────────────

    # Handles OCR output where columns were split across lines:

    #   "GEN 004\nIntroduction to Computers\n3 2 1 3"

    lines = text.splitlines()

    for i, line in enumerate(lines):

        m = re.fullmatch(r"\s*([A-Z]{2,4})\s*(\d{3,4})\s*", line)

        if m:

            prefix, number = m.group(1), m.group(2)

            code = f"{prefix} {number}"

            if code in seen_codes:

                continue

            # Look at the next non-empty line for the title

            for j in range(i + 1, min(i + 3, len(lines))):

                candidate = lines[j].strip()

                # Title line: starts with a letter, not another code

                if (candidate

                        and re.match(r"[A-Za-z\u0600-\u06FF]", candidate)

                        and not re.match(r"[A-Z]{2,4}\s*\d{3,4}", candidate)):

                    _add(prefix, number, candidate)

                    break



    # ── Strategy D: bare code with no title (use translation table) ───────

    # Last resort — at least we know the course exists

    for m in _COURSE_CODE_RE.finditer(text):

        prefix, number = m.group(1), m.group(2)

        code = f"{prefix} {number}"

        if code not in seen_codes:

            # Only add if we have a known Arabic title for it

            from_table = _to_arabic_title(code)

            if from_table != code:   # translation found

                seen_codes.add(code)

                courses.append({"code": code, "title": from_table})



    return courses





def _normalize_ocr_text(text: str) -> str:

    """

    Normalize OCR-noisy text before course extraction.

    Handles:

    - Broken course codes: "G E N  0 0 4" → "GEN 004"

    - Multiple spaces between code parts: "GEN  004" → "GEN 004"

    - Mixed separators: "GEN-004", "GEN:004" → kept as-is (regex handles them)

    - Duplicate blank lines collapsed to single blank line

    """

    # Fix broken uppercase letter sequences: "G E N" → "GEN"

    # Pattern: single uppercase letters separated by spaces (2–4 letters)

    text = re.sub(

        r"\b([A-Z])\s([A-Z])\s([A-Z])\s([A-Z])\b",

        r"\1\2\3\4", text

    )

    text = re.sub(

        r"\b([A-Z])\s([A-Z])\s([A-Z])\b",

        r"\1\2\3", text

    )

    text = re.sub(

        r"\b([A-Z])\s([A-Z])\b(?=\s*\d)",

        r"\1\2", text

    )

    # Fix broken digit sequences: "0 0 4" → "004"

    text = re.sub(

        r"\b(\d)\s(\d)\s(\d)\s(\d)\b",

        r"\1\2\3\4", text

    )

    text = re.sub(

        r"\b(\d)\s(\d)\s(\d)\b",

        r"\1\2\3", text

    )

    # Collapse runs of 3+ spaces to a single space

    text = re.sub(r" {3,}", " ", text)

    # Collapse 3+ blank lines to one blank line

    text = re.sub(r"\n{3,}", "\n\n", text)

    return text





def _clean_ocr_noise(text: str) -> str:

    """

    OCR noise removal + normalization:

    - Fix broken letter/digit sequences

    - Collapse multiple spaces/tabs within a line

    - Remove lines that are only punctuation, digits, or ≤ 2 chars

    """

    # First normalize broken OCR sequences

    text = _normalize_ocr_text(text)



    lines   = text.splitlines()

    cleaned = []

    for line in lines:

        line = re.sub(r"[ \t]{2,}", " ", line).strip()

        if line and not re.fullmatch(r"[\W\d]{0,3}", line):

            cleaned.append(line)

    return "\n".join(cleaned)





def _preprocess_chunk(text: str) -> str:

    """

    Preprocess a single chunk before sending to the LLM.



    Steps:

      1. Clean OCR noise

      2. Extract course entries via all 4 strategies → format as bullet list

      3. Preserve level/semester header lines above the bullet list

      4. If no courses found, return cleaned text as-is



    Threshold: even 1 detected course triggers structured output.

    """

    cleaned = _clean_ocr_noise(text)

    courses = extract_courses_from_text(cleaned)



    # Debug: log what was extracted

    if courses:

        logger.debug(

            f"[preprocess] Extracted {len(courses)} courses: "

            + ", ".join(f"{c['code']}" for c in courses[:5])

        )

    else:

        # Log first 200 chars so we can see what the raw text looks like

        logger.debug(f"[preprocess] No courses found. Raw (200): {cleaned[:200]!r}")



    if not courses:

        return cleaned



    # Preserve level/semester header lines (e.g. "Level 1 Semester 1")

    header_lines = [

        line for line in cleaned.splitlines()

        if _LEVEL_SEM_RE.search(line) and not _COURSE_CODE_RE.search(line)

    ]



    parts: list[str] = []

    if header_lines:

        parts.append("\n".join(header_lines))



    parts.append("Courses:")

    for c in courses:

        parts.append(f"- {c['title']} ({c['code']})")



    result = "\n".join(parts)

    logger.debug(f"[preprocess] Output (300): {result[:300]!r}")

    return result





# ---------------------------------------------------------------------------

# Course-title Arabic translation table

# Maps common English course titles to Arabic equivalents.

# Extend this dict as more courses are encountered.

# ---------------------------------------------------------------------------

_COURSE_AR_TITLES: dict[str, str] = {

    # GEN / general courses

    "introduction to computers":        "مقدمة في الحاسبات",

    "intro to computers":               "مقدمة في الحاسبات",

    "computer skills":                  "مهارات الحاسب",

    "english language":                 "اللغة الإنجليزية",

    "arabic language":                  "اللغة العربية",

    "islamic studies":                  "الدراسات الإسلامية",

    "national education":               "التربية الوطنية",

    "military sciences":                "العلوم العسكرية",

    # BAS / basic sciences

    "mathematics 1":                    "رياضيات 1",

    "mathematics 2":                    "رياضيات 2",

    "mathematics 3":                    "رياضيات 3",

    "calculus":                         "حساب التفاضل والتكامل",

    "physics 1":                        "فيزياء 1",

    "physics 2":                        "فيزياء 2",

    "chemistry":                        "كيمياء",

    "engineering chemistry":            "كيمياء هندسية",

    "statistics":                       "إحصاء",

    "probability and statistics":       "احتمالات وإحصاء",

    "discrete mathematics":             "رياضيات تقسيمية",

    "linear algebra":                   "جبر خطي",

    # BCS / computer science

    "programming 1":                    "برمجة 1",

    "programming 2":                    "برمجة 2",

    "introduction to programming":      "مقدمة في البرمجة",

    "data structures":                  "هياكل البيانات",

    "algorithms":                       "خوارزميات",

    "object oriented programming":      "برمجة كائنية",

    "database systems":                 "قواعد البيانات",

    "operating systems":                "أنظمة التشغيل",

    "computer networks":                "شبكات الحاسب",

    "software engineering":             "هندسة البرمجيات",

    "artificial intelligence":          "الذكاء الاصطناعي",

    "computer architecture":            "معمارية الحاسب",

    "digital logic":                    "منطق رقمي",

    "web programming":                  "برمجة الويب",

    "computer graphics":                "رسوميات الحاسب",

    "human computer interaction":       "تفاعل الإنسان والحاسب",

    "information security":             "أمن المعلومات",

    "machine learning":                 "تعلم الآلة",

    "computer organization":            "تنظيم الحاسب",

    "theory of computation":            "نظرية الحوسبة",

    "compiler design":                  "تصميم المترجمات",

    "numerical methods":                "طرق عددية",

    "project management":               "إدارة المشاريع",

    "graduation project":               "مشروع التخرج",

    "graduation project 1":             "مشروع التخرج 1",

    "graduation project 2":             "مشروع التخرج 2",

}



# CS-related course code prefixes (used for program filtering)

_CS_PREFIXES = {"BCS", "CS", "IT", "GEN", "BAS", "CIS", "CSC", "INF", "SWE"}



# Semester detection patterns (Arabic + English)

_SEM1_DETECT = re.compile(

    r"ترم\s*اول|الترم\s*الاول|فصل\s*اول|semester\s*1|first\s*semester|sem\s*1|term\s*1",

    re.IGNORECASE | re.UNICODE,

)

_SEM2_DETECT = re.compile(

    r"ترم\s*تاني|الترم\s*التاني|فصل\s*تاني|semester\s*2|second\s*semester|sem\s*2|term\s*2",

    re.IGNORECASE | re.UNICODE,

)



# Level/year detection patterns

_LEVEL_DETECT: dict[int, re.Pattern] = {

    1: re.compile(

        r"فرق[هة]\s*اول[هى]?|الفرق[هة]\s*الاول[هى]?|level\s*1|year\s*1|first\s*year|"

        r"المستو[يى]\s*الاول|السن[هة]\s*الاول[هى]?|\باوله?\b",

        re.IGNORECASE | re.UNICODE,

    ),

    2: re.compile(

        r"فرق[هة]\s*تاني[هة]?|الفرق[هة]\s*التاني[هة]?|level\s*2|year\s*2|second\s*year|"

        r"المستو[يى]\s*التاني|السن[هة]\s*التاني[هة]?",

        re.IGNORECASE | re.UNICODE,

    ),

    3: re.compile(

        r"فرق[هة]\s*تالت[هة]?|level\s*3|year\s*3|third\s*year|المستو[يى]\s*التالت",

        re.IGNORECASE | re.UNICODE,

    ),

    4: re.compile(

        r"فرق[هة]\s*رابع[هة]?|level\s*4|year\s*4|fourth\s*year|المستو[يى]\s*الرابع",

        re.IGNORECASE | re.UNICODE,

    ),

}



# Arabic ordinal headers for output

_LEVEL_AR = {1: "الأولى", 2: "الثانية", 3: "الثالثة", 4: "الرابعة"}

_SEM_AR   = {1: "الأول",  2: "الثاني"}





def _to_arabic_title(english_title: str) -> str:

    """

    Return the Arabic translation of an English course title if known,

    otherwise return the original title unchanged.

    """

    key = english_title.strip().lower()

    # Exact match

    if key in _COURSE_AR_TITLES:

        return _COURSE_AR_TITLES[key]

    # Prefix match (handles "Mathematics 2 (BAS 004)" → strip trailing code)

    key_clean = re.sub(r"\s*\([A-Z]{2,4}\s*\d{3,4}\)\s*$", "", key).strip()

    if key_clean in _COURSE_AR_TITLES:

        return _COURSE_AR_TITLES[key_clean]

    return english_title





def _detect_query_semester(question: str) -> int | None:

    """Return 1, 2, or None based on semester mention in the question."""

    norm = question.replace("أ", "ا").replace("إ", "ا").replace("آ", "ا")

    if _SEM1_DETECT.search(norm):

        return 1

    if _SEM2_DETECT.search(norm):

        return 2

    return None





def _detect_query_level(question: str) -> int | None:

    """Return 1–4 or None based on year/level mention in the question."""

    norm = question.replace("أ", "ا").replace("إ", "ا").replace("آ", "ا")

    for level, pat in _LEVEL_DETECT.items():

        if pat.search(norm):

            return level

    return None





def _is_cs_course(code: str) -> bool:

    """Return True if the course code prefix belongs to a CS program."""

    prefix = re.match(r"([A-Z]{2,4})", code.upper())

    return bool(prefix and prefix.group(1) in _CS_PREFIXES)





def _extract_direct_course_answer(

    context_str: str,

    question: str,

    query_language: str,

    query_intent: Optional[QueryIntent] = None,

) -> str:

    """

    Extract a structured course-list answer directly from the preprocessed

    context — without calling the LLM.



    Filtering rules (relaxed for high recall):

      - Blocks with NO level/semester label → ALWAYS included

      - Blocks WITH a label → only included if label matches the query

      - If filtering removes ALL entries → fall back to ALL entries (no filter)

      - If course codes exist in context → ALWAYS return a list



    Returns "" only if zero course entries exist anywhere in the context.

    """

    if "Courses:" not in context_str:

        # ── Fail-safe: extract directly from raw context ──────────────────

        raw_codes = _COURSE_CODE_RE.findall(context_str)

        if len(raw_codes) >= 1:

            logger.info(

                f"[chat] Fail-safe: found {len(raw_codes)} raw course codes "

                "in context without 'Courses:' — running direct extraction"

            )

            courses = extract_courses_from_text(context_str)

            if courses:

                cit = ""

                for line in context_str.splitlines():

                    if line.startswith("[") and "File:" in line:

                        cit = line.strip("[]")

                        break

                synthetic_ctx = (

                    f"[📋 COURSE LIST | {cit}]\n"

                    "Courses:\n"

                    + "\n".join(f"- {c['title']} ({c['code']})" for c in courses)

                )

                return _extract_direct_course_answer(

                    context_str    = synthetic_ctx,

                    question       = question,

                    query_language = query_language,

                    query_intent   = query_intent,

                )

        return ""



    # ── Detect what the query is asking for ──────────────────────────────

    q_semester = _detect_query_semester(question)

    q_level    = _detect_query_level(question)



    # ── Collect ALL entries first (no filtering yet) ──────────────────────

    all_entries: list[dict] = []

    citation = ""



    for block in context_str.split("---"):

        if "Courses:" not in block:

            continue



        block_text = block.strip()



        # Extract citation from header

        block_citation = ""

        for line in block_text.splitlines():

            if line.startswith("[") and "File:" in line:

                block_citation = line.strip("[]")

                if not citation:

                    citation = block_citation

                break



        # Detect this block's own level/semester labels

        block_sem   = None

        block_level = None

        for line in block_text.splitlines():

            if _SEM1_DETECT.search(line):

                block_sem = 1

            elif _SEM2_DETECT.search(line):

                block_sem = 2

            for lv, pat in _LEVEL_DETECT.items():

                if pat.search(line):

                    block_level = lv



        # Extract bullet lines from this block

        in_courses = False

        for line in block_text.splitlines():

            if line.strip() == "Courses:":

                in_courses = True

                continue

            if in_courses:

                stripped = line.strip()

                if stripped.startswith("- "):

                    code_match = re.search(r"\(([A-Z]{2,4}[\s\-]?\d{3,4})\)\s*$", stripped)

                    code = code_match.group(1) if code_match else ""

                    all_entries.append({

                        "bullet":      stripped,

                        "code":        code,

                        "block_sem":   block_sem,

                        "block_level": block_level,

                        "citation":    block_citation,

                        "has_label":   (block_sem is not None or block_level is not None),

                    })

                elif stripped and not stripped.startswith("-"):

                    in_courses = False



    logger.info(f"[chat] Course extraction: {len(all_entries)} entries before filtering")



    if not all_entries:

        return ""



    # ── Apply level/semester filtering ────────────────────────────────────

    # Rule: only filter a block when it HAS a label AND the label mismatches.

    # Blocks with NO label are always included.

    def _passes_filter(e: dict) -> bool:

        # Block has no labels → always include

        if not e["has_label"]:

            return True

        # Check semester: only reject if block IS labelled AND label mismatches

        if q_semester is not None and e["block_sem"] is not None:

            if e["block_sem"] != q_semester:

                return False

        # Check level: only reject if block IS labelled AND label mismatches

        if q_level is not None and e["block_level"] is not None:

            if e["block_level"] != q_level:

                return False

        return True



    filtered_entries = [e for e in all_entries if _passes_filter(e)]

    logger.info(f"[chat] Course extraction: {len(filtered_entries)} entries after filtering")



    # ── Fallback: if filtering dropped everything, use all entries ─────────

    if not filtered_entries:

        logger.warning(

            f"[chat] FILTER DROPPED ALL COURSES "

            f"(before={len(all_entries)}, after=0) — using unfiltered list"

        )

        filtered_entries = all_entries



    # ── Program filtering: prefer CS courses when mixed ───────────────────

    q_program = query_intent.program if query_intent else None
    if q_program in ("computer science", None):
        cs_entries  = [e for e in filtered_entries if _is_cs_course(e["code"])]
        use_entries = cs_entries if cs_entries else filtered_entries
    else:
        use_entries = filtered_entries



    # ── Deduplicate by code ───────────────────────────────────────────────

    seen_codes: set[str] = set()

    final_entries: list[dict] = []

    for e in use_entries:

        key = e["code"].replace(" ", "").replace("-", "").upper()

        if key not in seen_codes:

            seen_codes.add(key)

            final_entries.append(e)



    # Final guarantee: if we still have nothing, use everything deduplicated

    if not final_entries:

        logger.warning("[chat] CS filter dropped all entries — using full list")

        seen_codes.clear()

        for e in filtered_entries:

            key = e["code"].replace(" ", "").replace("-", "").upper()

            if key not in seen_codes:

                seen_codes.add(key)

                final_entries.append(e)



    if not final_entries:

        return ""



    # ── Build bullet lines with Arabic titles ─────────────────────────────

    bullet_lines: list[str] = []

    for e in final_entries:

        raw_bullet = e["bullet"]

        m = re.match(r"^-\s+(.+?)\s+\(([A-Z]{2,4}[\s\-]?\d{3,4})\)\s*$", raw_bullet)

        if m:

            title    = m.group(1).strip()

            code     = m.group(2).strip()

            ar_title = _to_arabic_title(title)

            bullet_lines.append(f"- {ar_title} ({code})")

        else:

            bullet_lines.append(raw_bullet)



    # ── Build the answer header ───────────────────────────────────────────

    if query_language == "arabic":

        sem_part   = f"الترم {_SEM_AR[q_semester]}"  if q_semester else "الترم الأول"

        level_part = f"الفرقة {_LEVEL_AR[q_level]}"  if q_level    else "الفرقة الأولى"

        header = f"مواد {sem_part} {level_part}:"

        footer = f"\n[المصدر: {citation}]" if citation else ""

    else:

        sem_part   = f"Semester {q_semester}" if q_semester else "Semester 1"

        level_part = f"Level {q_level}"       if q_level    else "Level 1"

        header = f"Courses for {level_part} {sem_part}:"

        footer = f"\n[Source: {citation}]" if citation else ""



    return f"{header}\n" + "\n".join(bullet_lines) + footer





def _build_answer_from_courses(

    courses: list[dict],

    question: str,

    query_language: str,

    meta: dict,

) -> str:

    """

    Build a formatted answer string directly from a list of course dicts.

    No dependency on "Courses:" markers or context formatting.



    Parameters

    ----------

    courses : list[dict]

        Each dict has "code" and "title" keys.

    question : str

        Original user question (used to detect semester/level for header).

    query_language : str

        "arabic" or "english".

    meta : dict

        Metadata from the first retrieved chunk (for citation).

    """

    q_semester = _detect_query_semester(question)

    q_level    = _detect_query_level(question)



    # Apply level/semester filtering only when the course has a detectable

    # label AND it mismatches. Courses with no label always pass.

    # (For direct extraction from raw text, we have no block-level labels,

    #  so all courses pass — filtering is skipped here intentionally.)



    # Prefer CS courses when mixed programs are present

    cs_courses  = [c for c in courses if _is_cs_course(c["code"])]

    use_courses = cs_courses if cs_courses else courses



    # Build bullet lines with Arabic title translation

    bullets: list[str] = []

    for c in use_courses:

        ar_title = _to_arabic_title(c["title"])

        bullets.append(f"- {ar_title} ({c['code']})")



    if not bullets:

        return ""



    # Build header

    fname    = meta.get("fileName", "")

    page     = meta.get("page", "")

    citation = f"File: {fname} | Page: {page}" if fname else ""



    if query_language == "arabic":

        sem_part   = f"الترم {_SEM_AR[q_semester]}"  if q_semester else "الترم الأول"

        level_part = f"الفرقة {_LEVEL_AR[q_level]}"  if q_level    else "الفرقة الأولى"

        header = f"مواد {sem_part} {level_part}:"

        footer = f"\n[المصدر: {citation}]" if citation else ""

    else:

        sem_part   = f"Semester {q_semester}" if q_semester else "Semester 1"

        level_part = f"Level {q_level}"       if q_level    else "Level 1"

        header = f"Courses for {level_part} {sem_part}:"

        footer = f"\n[Source: {citation}]" if citation else ""



    return f"{header}\n" + "\n".join(bullets) + footer





def _build_codes_only_answer(

    raw_codes: list[tuple],

    question: str,

    query_language: str,

    meta: dict,

) -> str:

    """

    Hard fallback: course codes were detected but no titles could be extracted.

    Returns a minimal list of just the codes so the system NEVER says "not found"

    when course data is present.



    Parameters

    ----------

    raw_codes : list[tuple]

        Output of _COURSE_CODE_RE.findall() — list of (prefix, number) tuples.

    """

    q_semester = _detect_query_semester(question)

    q_level    = _detect_query_level(question)



    # Deduplicate codes

    seen: set[str] = set()

    bullets: list[str] = []

    for prefix, number in raw_codes:

        code = f"{prefix} {number}".upper()

        if code not in seen:

            seen.add(code)

            # Try translation table first

            ar_title = _to_arabic_title(code)

            if ar_title != code:

                bullets.append(f"- {ar_title} ({code})")

            else:

                bullets.append(f"- {code}")



    if not bullets:

        return ""



    fname    = meta.get("fileName", "")

    page     = meta.get("page", "")

    citation = f"File: {fname} | Page: {page}" if fname else ""



    if query_language == "arabic":

        sem_part   = f"الترم {_SEM_AR[q_semester]}"  if q_semester else "الترم الأول"

        level_part = f"الفرقة {_LEVEL_AR[q_level]}"  if q_level    else "الفرقة الأولى"

        header = f"مواد {sem_part} {level_part} (مستخرجة من الجدول):"

        footer = f"\n[المصدر: {citation}]" if citation else ""

    else:

        sem_part   = f"Semester {q_semester}" if q_semester else "Semester 1"

        level_part = f"Level {q_level}"       if q_level    else "Level 1"

        header = f"Courses for {level_part} {sem_part} (extracted from table):"

        footer = f"\n[Source: {citation}]" if citation else ""



    return f"{header}\n" + "\n".join(bullets) + footer





# ---------------------------------------------------------------------------

# Helper: build context string from reranked results

# ---------------------------------------------------------------------------



def _build_context(results: list[dict], max_chars: int = 8000) -> str:

    """

    Build the context string sent to the LLM.



    Ordering priority (highest first):

      1. Chunks that contain level/semester markers AND course codes

         (e.g. "Level 1 Semester 1 … GEN 004 …")

      2. Table-boosted chunks (course codes present, no level marker)

      3. All other chunks by rerank score



    Merging: if two chunks from the same file share a level/semester header,

    their course lists are merged into one block so the LLM sees a single

    complete list instead of two fragments.

    """

    # ── Step 1: preprocess every chunk ───────────────────────────────────

    processed: list[dict] = []

    for r in results:

        raw = r.get("text", "").strip()

        if not raw:

            continue

        text = _preprocess_chunk(raw)

        has_courses    = "Courses:" in text

        has_level_sem  = bool(_LEVEL_SEM_RE.search(text))

        processed.append({

            **r,

            "_processed_text": text,

            "_has_courses":    has_courses,

            "_has_level_sem":  has_level_sem,

            "_priority": (

                0 if (has_courses and has_level_sem) else   # best: structured + labelled

                1 if has_courses else                        # good: structured

                2 if has_level_sem else                      # ok: labelled

                3                                            # generic

            ),

        })



    # ── Step 2: sort by priority then rerank score ────────────────────────

    processed.sort(key=lambda r: (

        r["_priority"],

        -r.get("rerank_score", r.get("boosted_score", 0.0)),

    ))



    # ── Step 3: merge course lists from the same file ─────────────────────

    # If multiple chunks from the same file all have "Courses:", combine

    # their bullet lines under one header so the LLM sees one complete list.

    merged_courses: dict[str, list[str]] = {}   # fname → [bullet, …]

    merged_meta:    dict[str, dict]      = {}   # fname → first chunk's meta

    non_course_blocks: list[dict]        = []



    for r in processed:

        text = r["_processed_text"]

        if r["_has_courses"]:

            meta  = r.get("metadata", {})

            fname = meta.get("fileName", "Unknown")

            if fname not in merged_courses:

                merged_courses[fname] = []

                merged_meta[fname]    = meta

            # Extract bullet lines from this chunk

            for line in text.splitlines():

                if line.startswith("- ") and line not in merged_courses[fname]:

                    merged_courses[fname].append(line)

        else:

            non_course_blocks.append(r)



    # ── Step 4: build final context string ────────────────────────────────

    parts: list[str] = []

    total = 0

    seen_text: set[str] = set()



    # Course-list blocks first (merged per file)

    for fname, bullets in merged_courses.items():

        if not bullets:

            continue

        meta    = merged_meta[fname]

        page    = meta.get("page", "N/A")

        section = meta.get("sectionTitle", "") or "N/A"

        header  = (

            f"[📋 COURSE LIST | File: {fname} | Page: {page} | Section: {section}]"

        )

        block = header + "\nCourses:\n" + "\n".join(bullets)

        if total + len(block) <= max_chars:

            parts.append(block)

            total += len(block)



    # Non-course blocks after

    for r in non_course_blocks:

        text = r["_processed_text"].strip()

        if not text:

            continue

        key = text[:120]

        if key in seen_text:

            continue

        seen_text.add(key)



        meta    = r.get("metadata", {})

        fname   = meta.get("fileName", "Unknown")

        page    = meta.get("page", "N/A")

        section = meta.get("sectionTitle", "") or "N/A"

        score   = r.get("rerank_score", r.get("boosted_score", 0.0))

        header  = (

            f"[Chunk | Score: {score:.3f} | "

            f"File: {fname} | Page: {page} | Section: {section}]"

        )

        block = f"{header}\n{text}"

        if total + len(block) > max_chars:

            break

        parts.append(block)

        total += len(block)



    return "\n\n---\n\n".join(parts)





def _build_sources(results: list[dict]) -> list[SourceInfo]:

    seen: set[str] = set()

    sources = []

    for r in results:

        meta = r.get("metadata", {})

        key  = f"{meta.get('fileName', '')}::{meta.get('page', '')}"

        if key in seen:

            continue

        seen.add(key)

        snippet_text = r["text"][:300] + ("…" if len(r["text"]) > 300 else "")
        sources.append(SourceInfo(

            file         = meta.get("fileName", "Unknown"),

            page         = int(meta.get("page", 0)),

            sectionTitle = meta.get("sectionTitle", ""),

            language     = meta.get("language", ""),

            keywords     = meta.get("keywords", ""),

            snippet      = snippet_text,

            textSnippet  = snippet_text,  # Added for Flutter client compatibility

            rerankScore  = round(r.get("rerank_score", 0.0), 4),

        ))

    return sources





@router.post("/", response_model=ChatResponse)
async def chat(request: ChatRequest):
    """
    RAG chat endpoint with hybrid retrieval and reranking.
    """
    if not request.question.strip():
        raise HTTPException(status_code=400, detail="Question cannot be empty")




    if collection is None:

        raise HTTPException(

            status_code=400,

            detail="No documents indexed yet. Please upload PDFs first.",

        )



    if collection.count() == 0:

        raise HTTPException(

            status_code=400,

            detail="No documents indexed yet. Please upload PDFs first.",

        )



    if embeddings is None:

        raise HTTPException(

            status_code=500,

            detail="Embeddings not configured. Set OPENAI_API_KEY or OPENROUTER_API_KEY in .env",

        )



    t_start = time.perf_counter()



    # ── 1. Query understanding (NEW) ──────────────────────────────────────

    query_intent = understand_query(request.question, llm=llm)



    # Allow explicit overrides from the request

    if request.program_filter:

        query_intent.program = request.program_filter

    if request.year_filter:

        query_intent.year = request.year_filter



    # ── 1c. CLARIFY — ask user to specify faculty when needed ────────────
    # If the query is about a faculty-specific topic but no faculty/program
    # was detected, return a clarification prompt listing available colleges.
    # This mirrors the CLI _route_query CLARIFY logic exactly.

    _REQUIRES_FACULTY_KEYWORDS = [
        # Arabic
        "ساعة", "ساعات", "مواد", "المواد", "مقررات", "المقررات", "منهج", "المنهج", "خطة", "الخطة",
        "غياب", "الغياب", "حرمان", "الحرمان", "تخرج", "التخرج", "الخريج",
        "نجاح", "النجاح", "رسوب", "الرسوب", "درجات", "الدرجات", "تقدير", "التقدير", "تقديرات", "التقديرات",
        "تحويل", "التحويل", "عذر", "العذر", "أعذار", "الأعذار", "اعذار", "الاعذار",
        "سحب", "السحب", "انسحاب", "الانسحاب", "انسحب", "مقرر", "المقرر", "مادة", "المادة",
        "تدريب صيفي", "التدريب الصيفي", "تدريب ميداني", "التدريب الميداني", "تدريب عملي", "التدريب العملي",
        "شروط", "الشروط", "شروط النجاح", "شروط التخرج", "شروط القبول", "متطلبات التخرج", "متطلبات", "المتطلبات",
        # English
        "courses", "subjects", "credit hours", "graduation requirements",
        "passing", "grading", "gpa", "attendance", "absence",
        "transfer", "excuses", "summer training", "field training",
        "withdraw", "withdrawal", "drop",
    ]

    _GENERAL_UNIV_KEYWORDS = [
        "الجامعة", "جامعة المنوفية", "رئيس", "رؤية", "رسالة",
        "الرسوم", "المصروفات", "شروط القبول", "الكليات المتاحة",
        "اللائحة العامة", "لائحة الجامعة", "university president",
        "الانسحاب العام", "registration", "general regulation",
    ]

    _q_lower = request.question.lower()
    _has_program = query_intent.program is not None
    _requires_faculty = any(kw in _q_lower for kw in _REQUIRES_FACULTY_KEYWORDS)
    _is_general_univ  = any(kw in _q_lower for kw in _GENERAL_UNIV_KEYWORDS)
    _query_lang_quick = detect_language(request.question)

    if _requires_faculty and not _has_program and not _is_general_univ:
        # Build the clarification message
        _AVAILABLE_FACULTIES_AR = (
            "• كلية الحاسبات والذكاء الاصطناعي\n"
            "• كلية الهندسة\n"
            "• كلية الصيدلة\n"
            "• كلية التمريض\n"
            "• كلية تكنولوجيا العلوم الصحية التطبيقية\n"
            "• كلية العلوم الإنسانية والاجتماعية\n"
            "• كلية طب الأسنان\n"
            "• كلية الطب البشري\n"
            "• كلية العلاج الطبيعي\n"
            "• كلية الطب البيطري"
        )
        _AVAILABLE_FACULTIES_EN = (
            "• Faculty of Computers & Artificial Intelligence\n"
            "• Faculty of Engineering\n"
            "• Faculty of Pharmacy\n"
            "• Faculty of Nursing\n"
            "• Faculty of Applied Health Sciences Technology\n"
            "• Faculty of Humanities & Social Sciences\n"
            "• Faculty of Dentistry\n"
            "• Faculty of Medicine\n"
            "• Faculty of Physical Therapy\n"
            "• Faculty of Veterinary Medicine"
        )
        if _query_lang_quick == "arabic":
            _clarify_msg = (
                "📌 سؤالك يحتاج تحديد الكلية أو البرنامج.\n\n"
                "الكليات المتاحة:\n"
                f"{_AVAILABLE_FACULTIES_AR}\n\n"
                "💡 أعد صياغة سؤالك مع ذكر الكلية، مثال:\n"
                "   «ما شروط النجاح في **كلية الحاسبات**؟»"
            )
        else:
            _clarify_msg = (
                "📌 Your question requires specifying a faculty or program.\n\n"
                "Available faculties:\n"
                f"{_AVAILABLE_FACULTIES_EN}\n\n"
                "💡 Please restate your question with the faculty name, e.g.:\n"
                "   \"What are the graduation requirements for the **Faculty of Computers**?\""
            )

        logger.info(f"[chat] CLARIFY — no faculty detected for faculty-specific query: '{request.question[:60]}'")

        _clarify_intent = {
            "program":    None,
            "year":       None,
            "semester":   None,
            "intent":     "clarify",
            "confidence": 1.0,
            "clean_query": request.question,
        }
        return ChatResponse(
            success=True,
            answer=_clarify_msg,
            sources=[],
            queryLanguage=_query_lang_quick,
            queryIntent=_clarify_intent,
            answerVerdict={"verdict": "CLARIFY", "label": "needs_faculty", "verified": True},
            retrievalStats={},
        )

    # Use the rewritten clean_query for embedding

    query_normalized = query_intent.clean_query



    # ── 1b. LLM query rewrite ────────────────────────────────────────────

    # Always run the LLM rewrite for two cases:

    #   a) Low-confidence intent (< 0.55) — rule-based parser found nothing

    #   b) High-confidence structured intent — the rule-based clean_query

    #      is a long mixed Arabic+English string that confuses ada-002.

    #      The LLM produces a tighter, more focused search query.

    # Skip only for medium confidence (0.55–0.75) where the rule-based

    # query is already clean and the LLM rewrite adds latency without gain.

    should_rewrite = (

        query_intent.confidence < 0.55          # low confidence: parser failed

        or query_intent.confidence >= 0.80      # high confidence: clean it up

    )

    if should_rewrite:

        rewritten = rewrite_query(request.question)

        if rewritten and rewritten.strip() != request.question.strip():

            query_normalized = rewritten

            query_intent.clean_query = rewritten

            logger.info(f"[chat] Query rewritten (conf={query_intent.confidence:.2f}): '{rewritten[:60]}'")



    logger.info(

        f"[chat] Intent: program={query_intent.program} year={query_intent.year} "

        f"intent={query_intent.intent} conf={query_intent.confidence:.2f} "

        f"clean='{query_normalized[:60]}'"

    )



    # ── 2. Embed query (with cache) ───────────────────────────────────────


    try:

        embedding_cache.embed_query_cached(query_normalized, embeddings)

    except Exception as e:

        raise HTTPException(

            status_code=500,

            detail=f"Embedding failed: {e}. Check your API key and credits.",

        )



    # ── 3. Hybrid retrieval (with query understanding + section boosting) ─

    bm25_idx = _get_bm25_index()



    query_language = detect_language(query_normalized)

    try:

        # ── EN→AR translation for better Arabic-doc retrieval ─────────────
        # English queries produce vectors far from Arabic document chunks.
        # We translate once and pass the Arabic version to hybrid_search
        # for retrieval only; the original question is kept for LLM response.
        _retrieval_query = _translate_query_to_arabic(
            query_normalized, query_language=query_language
        ) if query_language != "arabic" else query_normalized

        # Dynamic top_k/candidates_k scaling for subjects list and summer training
        _is_subj = (query_intent.intent in ("subjects list", "curriculum"))
        _is_training = (
            query_intent.intent == "regulations"
            and any(kw in request.question.lower() for kw in ["تدريب", "training", "internship", "صيفي", "ميداني", "عملي"])
        )

        if _is_subj:
            target_top_k = 20
            candidates_k = 200
            final_top_k  = 20
        elif _is_training:
            target_top_k = 15
            candidates_k = 120
            final_top_k  = 12
        else:
            target_top_k = max(request.top_k * 4, 8)
            candidates_k = max(request.top_k * 6, 80)
            final_top_k  = request.top_k

        retrieval_output = hybrid_search(

            query                  = _retrieval_query,

            collection             = collection,

            embeddings             = embeddings,

            bm25_index             = bm25_idx,

            top_k                  = target_top_k,

            dense_candidates       = candidates_k,

            sparse_candidates      = candidates_k,

            use_rrf                = True,

            language_filter        = request.language_filter,

            section_filter         = request.section_filter,

            file_filter            = request.file_filter,

            use_query_understanding = True,

            use_multi_query        = True,

            use_section_boost      = True,

            query_intent           = query_intent,

            llm                    = llm,

        )

    except Exception as e:

        logger.error(f"[chat] Hybrid search failed: {e}", exc_info=True)

        raise HTTPException(status_code=500, detail=f"Retrieval failed: {e}")



    candidates      = retrieval_output["results"]

    query_language  = retrieval_output["query_language"]

    retrieval_stats = retrieval_output["retrieval_stats"]



    # Build intent dict for response

    intent_dict = {

        "program":    query_intent.program,

        "year":       query_intent.year,

        "semester":   query_intent.semester,

        "intent":     query_intent.intent,

        "confidence": round(query_intent.confidence, 2),

        "clean_query": query_intent.clean_query,

    }



    if not candidates:

        no_info = (

            "عذرًا، هذه المعلومة غير متوفرة في اللوائح الحالية المرفقة لدينا. "
            "لمزيد من الاستفسارات يُمكنك التواصل مع إدارة الكلية أو شؤون الطلاب المختصة."

            if query_language == "arabic"

            else "Sorry, this information is not available in the currently provided regulations. "
                 "For further inquiries, please contact the respective faculty administration "
                 "or student affairs office."

        )

        return ChatResponse(

            success=True,

            answer=no_info,

            sources=[],

            queryLanguage=query_language,

            queryIntent=intent_dict,

            answerVerdict={"verdict": "NO", "label": "not_supported", "verified": False},

            retrievalStats=retrieval_stats,

        )



    # ── 4. Rerank (intent-aware, bilingual) ──────────────────────────────

    rerank_method = (request.rerank_method or "cosine").lower()



    if rerank_method == "none":

        reranked = candidates[:final_top_k]

        for i, c in enumerate(reranked):

            c["rerank_score"]  = c.get("boosted_score", c.get("rrf_score", 1.0 - i * 0.01))

            c["rerank_method"] = "passthrough"

    elif rerank_method == "llm":

        reranked = rerank_results(

            query          = query_normalized,

            candidates     = candidates,

            llm            = llm,

            top_n          = final_top_k,

            query_intent   = query_intent,

            original_query = request.question,

        )

    else:  # default: cosine (bilingual)

        reranked = rerank_results(

            query          = query_normalized,

            candidates     = candidates,

            embeddings     = embeddings,

            top_n          = final_top_k,

            original_query = request.question,  # enables bilingual scoring

        )



    if not reranked:

        no_info = (

            "عذرًا، هذه المعلومة غير متوفرة في اللوائح الحالية المرفقة لدينا. "
            "لمزيد من الاستفسارات يُمكنك التواصل مع إدارة الكلية أو شؤون الطلاب المختصة."

            if query_language == "arabic"

            else "Sorry, this information is not available in the currently provided regulations. "
                 "For further inquiries, please contact the respective faculty administration "
                 "or student affairs office."

        )

        return ChatResponse(

            success=True,

            answer=no_info,

            sources=[],

            queryLanguage=query_language,

            queryIntent=intent_dict,

            answerVerdict={"verdict": "NO", "label": "not_supported", "verified": False},

            retrievalStats=retrieval_stats,

        )



    # ── 5. Build context and generate answer ──────────────────────────────



    # ── 5a. GUARANTEED COURSE EXTRACTION (runs before LLM, before context) ──

    # Detection is per-chunk on RAW text only.

    # Reason the old approach failed: joining chunks and normalizing text

    # could distort numeric patterns (e.g. "3 5 3" → "353") and break

    # word-boundary anchors (\b) in mixed Arabic+English content.

    # Fix: scan each chunk's raw text individually with a boundary-free regex.



    # Flexible course-code regex — no \b anchors, works on mixed OCR text

    _DETECT_RE = re.compile(r"([A-Z]{2,4})\s*(\d{3,4})", re.IGNORECASE)



    _should_trigger  = False

    _detected_codes: list[tuple] = []   # (prefix, number) from all chunks

    _first_meta:     dict        = {}



    for _r in reranked:

        _chunk_raw = _r.get("text", "")

        if not _chunk_raw:

            continue

        if not _first_meta:

            _first_meta = _r.get("metadata", {})

        _matches = _DETECT_RE.findall(_chunk_raw)

        if _matches:

            _should_trigger = True

            _detected_codes.extend(_matches)



    # Collect all raw text and norm for logging only (not used for detection)

    _all_raw_text = "\n".join(r.get("text", "") for r in reranked)

    _all_raw_norm = _normalize_ocr_text(_all_raw_text)



    logger.debug(
        f"\n=== DEBUG FULL PIPELINE ===\n"

        f"Question: {request.question}\n"

        f"RAW CHUNKS COUNT: {len(reranked)}\n"

        f"RAW TEXT SAMPLE:\n{_all_raw_text[:500]}\n"

        f"CODES FOUND (per-chunk): {_detected_codes[:15]}\n"

        f"SHOULD TRIGGER: {_should_trigger}\n"

        f"=========================="

    )



    # Only trigger guaranteed course extraction for explicit subject-list queries.

    # For medicine/engineering regulation questions (intent=general/graduation/grades),

    # skip this shortcut and let the LLM answer from context properly.

    # Academic advising guard: if the query contains GPA, warning, probation, failed courses,
    # or prerequisite questions, do NOT use the direct course-list shortcut.
    # Let the LLM answer from context properly.
    _query_lower = request.question.lower()
    _is_advising_query = any(kw in _query_lower for kw in [
        # GPA / Advising
        "gpa", "cgpa", "معدل", "جي بي", "إنذار", "انذار", "warning", "probation",
        # Failures / Prerequisites
        "رسوب", "راسب", "شايل", "ملحق", "شلت", "failed", "fail", "أقل من", "اقل من",
        "2.", "2,", "٢", "متطلب", "متطلبات", "prereq", "prerequisite",
        # Withdrawal / Drop
        "انسحاب", "الانسحاب", "سحب", "السحب", "withdraw", "withdrawal", "drop",
        # Rules / Conditions / Regulations
        "شرط", "شروط", "قواعد", "قاعدة", "لوائح", "لائحة", "نظام", "rules", "regulations", "policy", "قانون", "قوانين",
        # Transfer
        "تحويل", "التحويل", "نقل", "transfer",
        # Attendance / Absences
        "حرمان", "الحرمان", "حرم", "غياب", "الغياب", "حضور", "الحضور", "attendance", "absence", "missing",
        # General / Registration / Admission
        "تسجيل", "التسجيل", "قبول", "القبول", "registration", "enrollment", "admission"
    ])
    _is_subjects_query = (

        query_intent.intent == "subjects list"

        and query_intent.confidence >= 0.50

        and not _is_advising_query

    )

    if _should_trigger and _is_subjects_query:

        # ── Step 1: extract courses from every raw chunk ──────────────────

        _all_courses: list[dict] = []



        for r in reranked:

            raw = r.get("text", "").strip()

            if not raw:

                continue

            # Try normalized first, fall back to raw if normalization yields nothing

            norm_raw      = _normalize_ocr_text(raw)

            chunk_courses = extract_courses_from_text(norm_raw)

            if not chunk_courses:

                chunk_courses = extract_courses_from_text(raw)

            _all_courses.extend(chunk_courses)



        logger.info(

            f"[chat] Extracted {len(_all_courses)} courses BEFORE dedup "

            f"from {len(reranked)} chunks"

        )



        # ── Step 2: deduplicate ───────────────────────────────────────────

        _seen:   set[str]   = set()

        _deduped: list[dict] = []

        for c in _all_courses:

            key = c["code"].replace(" ", "").replace("-", "").upper()

            if key not in _seen:

                _seen.add(key)

                _deduped.append(c)



        logger.info(f"[chat] Final course count after dedup: {len(_deduped)}")



        # ── Step 3: build answer ──────────────────────────────────────────

        if _deduped:

            _direct_answer = _build_answer_from_courses(

                courses        = _deduped,

                question       = request.question,

                query_language = query_language,

                meta           = _first_meta,

            )

            # ── Step 4: return — ONLY if we have deduped courses ───────────────────

            logger.info(

                f"[chat] Course extraction complete (structured) — "

                f"returning answer (len={len(_direct_answer)})"

            )

            answer_verdict = {"verdict": "YES", "label": "fully_supported", "verified": True}

            t_total_ms = (time.perf_counter() - t_start) * 1000

            retrieval_stats["total_pipeline_ms"] = round(t_total_ms, 1)

            retrieval_stats["rerank_method"]     = rerank_method

            retrieval_stats["cache_stats"]       = embedding_cache.stats

            retrieval_stats["answer_verdict"]    = "YES"

            retrieval_stats["shortcut"]          = "course_extraction_guaranteed"

            log_retrieval_result(

                query           = request.question,

                results         = reranked,

                query_language  = query_language,

                retrieval_stats = retrieval_stats,

            )

            return ChatResponse(

                success        = True,

                answer         = _direct_answer,

                sources        = _build_sources(reranked),

                queryLanguage  = query_language,

                queryIntent    = intent_dict,

                answerVerdict  = answer_verdict,

                retrievalStats = retrieval_stats,

            )

        else:

            logger.info(

                "[chat] Subjects query triggered but no structured courses extracted. "

                "Falling back to standard LLM generation."

            )



    context_str = _build_context(reranked)



    # Academic advising guard: if the query contains GPA, warning, probation, failed courses,
    # or prerequisite questions, do NOT use the direct course-list shortcut.
    # Let the LLM answer from context properly.
    _query_lower = request.question.lower()
    _is_advising_query = any(kw in _query_lower for kw in [
        # GPA / Advising
        "gpa", "cgpa", "معدل", "جي بي", "إنذار", "انذار", "warning", "probation",
        # Failures / Prerequisites
        "رسوب", "راسب", "شايل", "ملحق", "شلت", "failed", "fail", "أقل من", "اقل من",
        "2.", "2,", "٢", "متطلب", "متطلبات", "prereq", "prerequisite",
        # Withdrawal / Drop
        "انسحاب", "الانسحاب", "سحب", "السحب", "withdraw", "withdrawal", "drop",
        # Rules / Conditions / Regulations
        "شرط", "شروط", "قواعد", "قاعدة", "لوائح", "لائحة", "نظام", "rules", "regulations", "policy", "قانون", "قوانين",
        # Transfer
        "تحويل", "التحويل", "نقل", "transfer",
        # Attendance / Absences
        "حرمان", "الحرمان", "حرم", "غياب", "الغياب", "حضور", "الحضور", "attendance", "absence", "missing",
        # General / Registration / Admission
        "تسجيل", "التسجيل", "قبول", "القبول", "registration", "enrollment", "admission"
    ])
    _is_subjects_query = (
        query_intent.intent == "subjects list"
        and query_intent.confidence >= 0.50
        and not _is_advising_query
    )
    _direct_answer = ""
    if _is_subjects_query:
        _direct_answer = _extract_direct_course_answer(
            context_str    = context_str,
            question       = request.question,
            query_language = query_language,
            query_intent   = query_intent,
        )

    if _direct_answer:

        logger.info("[chat] Course-list shortcut (post-context): returning direct answer without LLM")

        answer_verdict = {"verdict": "YES", "label": "fully_supported", "verified": True}



        t_total_ms = (time.perf_counter() - t_start) * 1000

        retrieval_stats["total_pipeline_ms"] = round(t_total_ms, 1)

        retrieval_stats["rerank_method"]     = rerank_method

        retrieval_stats["cache_stats"]       = embedding_cache.stats

        retrieval_stats["answer_verdict"]    = "YES"

        retrieval_stats["shortcut"]          = "course_list_post_context"



        log_retrieval_result(

            query           = request.question,

            results         = reranked,

            query_language  = query_language,

            retrieval_stats = retrieval_stats,

        )

        return ChatResponse(

            success        = True,

            answer         = _direct_answer,

            sources        = _build_sources(reranked),

            queryLanguage  = query_language,

            queryIntent    = intent_dict,

            answerVerdict  = answer_verdict,

            retrievalStats = retrieval_stats,

        )



    llm_failed = False
    try:
        # ── Choose prompt template based on query intent ──────────────────
        # Use the shorter, precision-focused legal prompt for regulation/policy
        # questions; use the full few-shot main prompt for everything else.
        _legal_intents = {"regulation", "policy", "graduation", "grading", "attendance",
                          "withdrawal", "exam", "scholarship", "rules"}
        _detected_intent = (query_intent.intent or "").lower()
        _is_legal = (
            any(kw in _detected_intent for kw in _legal_intents)
            or any(kw in request.question for kw in
                   ("لائحة", "نظام تقييم", "تخرج", "رسوب", "غياب", "عقوبة",
                    "انذار", "نظام", "regulation", "policy", "grading", "graduation"))
        )
        _template = _PROMPT_LEGAL if _is_legal else _PROMPT
        prompt  = _template.format(context=context_str, question=request.question)

        response = llm.invoke(prompt)

        answer   = response.content if hasattr(response, "content") else str(response)

    except Exception as e:

        logger.error(f"[chat] LLM generation failed: {e}", exc_info=True)

        llm_failed = True
        if query_language == "arabic":
            answer = "⚠️ عذراً، تعذر الاتصال بنموذج الذكاء الاصطناعي لتلخيص الإجابة (تجاوز حد الاستخدام لليوم). ولكن تم استخراج النصوص التالية مباشرة من لائحة البرنامج كمصادر للإجابة:\n\n"
        else:
            answer = "⚠️ Sorry, the AI model could not be reached to summarize the answer (rate limit exceeded). However, the following text segments were extracted directly from the regulation documents as sources:\n\n"
        
        for idx, chunk in enumerate(reranked[:3], 1):
            fname = chunk.get("metadata", {}).get("fileName", "Document")
            page = chunk.get("metadata", {}).get("page", "?")
            text = chunk.get("text", "").strip()
            answer += f"📄 **المصدر [{idx}]: {fname} - صفحة {page}**\n{text}\n\n" 



    # ── 5c. Answer verification + smart fallback ─────────────────────────

    # Check whether the generated answer is actually supported by the

    # retrieved context.

    #

    # Smart fallback logic:

    #   If the LLM said "not found" BUT the verifier says the context IS

    #   relevant (PARTIAL/YES), the LLM was being overly strict.

    #   We retry once with a more permissive prompt that explicitly asks

    #   the LLM to extract implicit/partial information.

    _not_found_markers = (
        "المعلومات غير موجودة",
        "المعلومات غير متوفرة",
        "لا تتوفر معلومات",
        "لم أجد",
        "غير متاحة في السياق",
        "عذرًا، هذه المعلومة غير متوفرة",  # new polite fallback
        "Information not found",
        "not available in the context",
        "no information",
        "Sorry, this information is not available",  # new polite fallback
    )

    if llm_failed:
        answer_verdict = {"verdict": "YES", "label": "fully_supported", "verified": True}
        answer_is_not_found = False
    else:
        answer_is_not_found = any(m in answer for m in _not_found_markers)



    if not llm_failed and answer_is_not_found:

        # Quick check: does the context actually contain relevant content?

        fallback_verdict = verify_answer(

            question = request.question,

            context  = context_str,

            answer   = answer,

        )

        if fallback_verdict["verdict"] in ("YES", "PARTIAL"):

            # Context IS relevant — the LLM was too strict. Retry with a

            # permissive prompt that explicitly allows partial answers.

            logger.info("[chat] Smart fallback: LLM said not-found but context is relevant — retrying")

            try:

                fallback_prompt = _FALLBACK_PROMPT.format(

                    context  = context_str,

                    question = request.question,

                )

                fallback_response = llm.invoke(fallback_prompt)

                fallback_answer   = (

                    fallback_response.content

                    if hasattr(fallback_response, "content")

                    else str(fallback_response)

                )

                # Only use the fallback if it actually produced content

                if fallback_answer.strip() and not any(

                    m in fallback_answer for m in _not_found_markers

                ):

                    answer = fallback_answer

                    answer_is_not_found = False

                    logger.info("[chat] Smart fallback succeeded")

            except Exception as fb_err:

                logger.warning(f"[chat] Smart fallback LLM call failed: {fb_err}")

        answer_verdict = fallback_verdict

    else:
        should_verify = (
            query_intent.confidence < 0.70
            or query_intent.intent == "general"
        )
        if answer_is_not_found or should_verify:
            answer_verdict = verify_answer(
                question = request.question,
                context  = context_str,
                answer   = answer,
            )
        else:
            answer_verdict = {"verdict": "YES", "label": "fully_supported", "verified": True}

        if answer_verdict["verdict"] == "NO":

            # Verifier says not supported — mark accordingly but keep answer

            logger.warning("[chat] Verifier flagged answer as unsupported (NO). Triggering Fallback...")

            try:

                fallback_prompt = _FALLBACK_PROMPT.format(

                    context  = context_str,

                    question = request.question,

                )

                fallback_response = llm.invoke(fallback_prompt)

                fallback_answer   = (

                    fallback_response.content

                    if hasattr(fallback_response, "content")

                    else str(fallback_response)

                ).strip()



                # Verify the fallback answer

                fallback_verdict = verify_answer(

                    question = request.question,

                    context  = context_str,

                    answer   = fallback_answer,

                )



                if fallback_verdict["verdict"] in ("YES", "PARTIAL") and not any(m in fallback_answer for m in _not_found_markers):

                    answer = fallback_answer

                    answer_verdict = fallback_verdict

                    logger.info("[chat] Fallback recovery succeeded after initial NO verdict")

                else:

                    # Fallback also failed or was ungrounded. Refuse to answer with hallucination.

                    logger.warning("[chat] Fallback also ungrounded (or not found). Refusing to answer.")

                    refusal = (

                        "عذرًا، هذه المعلومة غير متوفرة في اللوائح الحالية المرفقة لدينا. لمزيد من الاستفسارات يُمكنك التواصل مع إدارة الكلية أو شؤون الطلاب المختصة."

                        if query_language == "arabic"

                        else "Sorry, this information is not available in the currently provided regulations. For further inquiries, please contact the respective faculty administration or student affairs office."

                    )

                    answer = refusal

                    answer_verdict = {"verdict": "NO", "label": "not_supported", "verified": False}

            except Exception as fb_err:

                logger.warning(f"[chat] Fallback LLM call failed: {fb_err}")

                refusal = (

                    "عذرًا، هذه المعلومة غير متوفرة في اللوائح الحالية المرفقة لدينا. لمزيد من الاستفسارات يُمكنك التواصل مع إدارة الكلية أو شؤون الطلاب المختصة."

                    if query_language == "arabic"

                    else "Sorry, this information is not available in the currently provided regulations. For further inquiries, please contact the respective faculty administration or student affairs office."
                )

                answer = refusal

                answer_verdict = {"verdict": "NO", "label": "not_supported", "verified": False}



    # ── 6. Log retrieval trace ────────────────────────────────────────────

    t_total_ms = (time.perf_counter() - t_start) * 1000

    retrieval_stats["total_pipeline_ms"] = round(t_total_ms, 1)

    retrieval_stats["rerank_method"]     = rerank_method

    retrieval_stats["cache_stats"]       = embedding_cache.stats

    retrieval_stats["answer_verdict"]    = answer_verdict["verdict"]



    log_retrieval_result(

        query           = request.question,

        results         = reranked,

        query_language  = query_language,

        retrieval_stats = retrieval_stats,

    )



    logger.info(

        f"[chat] Done: lang={query_language}, chunks={len(reranked)}, "

        f"total={t_total_ms:.0f}ms"

    )



    return ChatResponse(

        success        = True,

        answer         = answer,

        sources        = _build_sources(reranked),

        queryLanguage  = query_language,

        queryIntent    = intent_dict,

        answerVerdict  = answer_verdict,

        retrievalStats = retrieval_stats,

    )





# ---------------------------------------------------------------------------

# Course catalog endpoints

# ---------------------------------------------------------------------------



@router.get("/courses")

async def get_courses(

    year:     Optional[int] = Query(default=None, ge=1, le=4),

    semester: Optional[int] = Query(default=None, ge=1, le=2),

    program:  Optional[str] = Query(default=None),

    file:     Optional[str] = Query(default=None),

):

    """

    Return courses from the dynamic catalog.



    Query params (all optional):

        year      – academic year 1–4

        semester  – semester 1 or 2

        program   – e.g. "computer science"

        file      – filter by source file name (substring)



    Example:

        GET /api/chat/courses?year=1&semester=1&program=computer+science

    """

    catalog = get_catalog(collection)

    if not catalog.is_built:

        catalog.build()



    courses = catalog.query(year=year, semester=semester, program=program, file=file)

    return JSONResponse({

        "success": True,

        "count":   len(courses),

        "filters": {"year": year, "semester": semester, "program": program, "file": file},

        "courses": courses,

    })





@router.get("/courses/summary")

async def get_courses_summary():

    """Return a summary of the course catalog (totals by program and year)."""

    catalog = get_catalog(collection)

    if not catalog.is_built:

        catalog.build()

    return JSONResponse({"success": True, **catalog.summary()})





@router.post("/courses/rebuild")

async def rebuild_course_catalog():

    """Force-rebuild the course catalog from ChromaDB."""

    catalog = get_catalog(collection, force_rebuild=True)

    return JSONResponse({

        "success": True,

        "message": f"Catalog rebuilt: {len(catalog.courses)} courses",

        **catalog.summary(),

    })





# ---------------------------------------------------------------------------

# Admin endpoints

# ---------------------------------------------------------------------------



@router.post("/rebuild-index")

async def rebuild_bm25_index():

    """Force-rebuild the BM25 index from the current ChromaDB collection."""

    if collection is None:

        raise HTTPException(status_code=400, detail="No collection available")

    try:

        invalidate_index(COLLECTION_NAME)

        idx = _get_bm25_index(force_rebuild=True)

        return JSONResponse({

            "success": True,

            "message": f"BM25 index rebuilt with {idx.doc_count} documents",

        })

    except Exception as e:

        raise HTTPException(status_code=500, detail=str(e))





@router.get("/retrieval-stats")

async def get_retrieval_stats():

    """Return cache statistics and BM25 index info."""

    bm25_idx = _get_bm25_index()

    return JSONResponse({

        "embedding_cache": embedding_cache.stats,

        "bm25_index": {

            "built":     bm25_idx.is_built if bm25_idx else False,

            "doc_count": bm25_idx.doc_count if bm25_idx else 0,

        },

        "collection": {

            "name":  COLLECTION_NAME,

            "count": collection.count() if collection else 0,

        },

    })





@router.get("/retrieval-logs")

async def get_retrieval_logs(

    last_n: int = Query(default=20, ge=1, le=200, description="Number of recent log entries")

):

    """Return the last N retrieval log entries."""

    import json

    from pathlib import Path



    log_path = Path("./retrieval_logs/retrieval_trace.jsonl")

    if not log_path.exists():

        return JSONResponse({"logs": [], "total": 0})



    try:

        lines = log_path.read_text(encoding="utf-8").strip().splitlines()

        recent = lines[-last_n:]

        logs   = [json.loads(line) for line in recent]

        return JSONResponse({"logs": logs, "total": len(lines)})

    except Exception as e:

        raise HTTPException(status_code=500, detail=str(e))