fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pypdf>=3.17.1
chromadb>=0.4.18
langchain>=0.1.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.1.0
openai>=1.6.1
python-dotenv>=1.0.0
pydantic>=2.5.2
numpy>=1.26.2
pandas>=2.0.0

# ── OCR pipeline ──────────────────────────────────────────────────────────────
# PyMuPDF: native text extraction + page rendering for OCR (already in venv)
pymupdf>=1.23.0
# Tesseract Python wrapper + image support
pytesseract>=0.3.10
Pillow>=10.0.0
# pdfplumber: accurate table extraction from PDFs (handles Arabic tables)
pdfplumber>=0.10.3

# ── Arabic NLP (optional but recommended) ────────────────────────────────────
# pyarabic: deeper Arabic normalization (graceful fallback if not installed)
pyarabic>=0.6.15

# ── Hybrid retrieval ──────────────────────────────────────────────────────────
# BM25 sparse retrieval (preferred over TF-IDF)
rank-bm25>=0.2.2
# TF-IDF fallback
scikit-learn>=1.3.0

# ── Reranking (optional – cross-encoder strategy) ────────────────────────────
# Uncomment to enable cross-encoder reranking (~500MB model download on first use)
# sentence-transformers>=2.6.0

# ── System requirement (NOT a pip package) ───────────────────────────────────
# Tesseract OCR binary must be installed on the OS:
#   Windows : https://github.com/UB-Mannheim/tesseract/wiki
#             (tick "Arabic" language data during installation)
#   Linux   : sudo apt-get install tesseract-ocr tesseract-ocr-ara
#   macOS   : brew install tesseract tesseract-lang

